// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/abortiontype"
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// AbortionType is the model entity for the AbortionType schema.
type AbortionType struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 名称
	Name string `json:"name,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AbortionType) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case abortiontype.FieldID:
			values[i] = new(sql.NullInt64)
		case abortiontype.FieldName:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type AbortionType", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AbortionType fields.
func (at *AbortionType) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case abortiontype.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			at.ID = int(value.Int64)
		case abortiontype.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				at.Name = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this AbortionType.
// Note that you need to call AbortionType.Unwrap() before calling this method if this AbortionType
// was returned from a transaction, and the transaction was committed or rolled back.
func (at *AbortionType) Update() *AbortionTypeUpdateOne {
	return (&AbortionTypeClient{config: at.config}).UpdateOne(at)
}

// Unwrap unwraps the AbortionType entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (at *AbortionType) Unwrap() *AbortionType {
	_tx, ok := at.config.driver.(*txDriver)
	if !ok {
		panic("ent: AbortionType is not a transactional entity")
	}
	at.config.driver = _tx.drv
	return at
}

// String implements the fmt.Stringer.
func (at *AbortionType) String() string {
	var builder strings.Builder
	builder.WriteString("AbortionType(")
	builder.WriteString(fmt.Sprintf("id=%v, ", at.ID))
	builder.WriteString("name=")
	builder.WriteString(at.Name)
	builder.WriteByte(')')
	return builder.String()
}

// AbortionTypes is a parsable slice of AbortionType.
type AbortionTypes []*AbortionType

func (at AbortionTypes) config(cfg config) {
	for _i := range at {
		at[_i].config = cfg
	}
}

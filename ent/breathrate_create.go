// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/breathrate"
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// BreathRateCreate is the builder for creating a BreathRate entity.
type BreathRateCreate struct {
	config
	mutation *BreathRateMutation
	hooks    []Hook
}

// SetName sets the "name" field.
func (brc *BreathRateCreate) SetName(s string) *BreathRateCreate {
	brc.mutation.SetName(s)
	return brc
}

// Mutation returns the BreathRateMutation object of the builder.
func (brc *BreathRateCreate) Mutation() *BreathRateMutation {
	return brc.mutation
}

// Save creates the BreathRate in the database.
func (brc *BreathRateCreate) Save(ctx context.Context) (*BreathRate, error) {
	var (
		err  error
		node *BreathRate
	)
	if len(brc.hooks) == 0 {
		if err = brc.check(); err != nil {
			return nil, err
		}
		node, err = brc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*BreathRateMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = brc.check(); err != nil {
				return nil, err
			}
			brc.mutation = mutation
			if node, err = brc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(brc.hooks) - 1; i >= 0; i-- {
			if brc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = brc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, brc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*BreathRate)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from BreathRateMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (brc *BreathRateCreate) SaveX(ctx context.Context) *BreathRate {
	v, err := brc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (brc *BreathRateCreate) Exec(ctx context.Context) error {
	_, err := brc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (brc *BreathRateCreate) ExecX(ctx context.Context) {
	if err := brc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (brc *BreathRateCreate) check() error {
	if _, ok := brc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "BreathRate.name"`)}
	}
	if v, ok := brc.mutation.Name(); ok {
		if err := breathrate.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf(`ent: validator failed for field "BreathRate.name": %w`, err)}
		}
	}
	return nil
}

func (brc *BreathRateCreate) sqlSave(ctx context.Context) (*BreathRate, error) {
	_node, _spec := brc.createSpec()
	if err := sqlgraph.CreateNode(ctx, brc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (brc *BreathRateCreate) createSpec() (*BreathRate, *sqlgraph.CreateSpec) {
	var (
		_node = &BreathRate{config: brc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: breathrate.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: breathrate.FieldID,
			},
		}
	)
	if value, ok := brc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: breathrate.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// BreathRateCreateBulk is the builder for creating many BreathRate entities in bulk.
type BreathRateCreateBulk struct {
	config
	builders []*BreathRateCreate
}

// Save creates the BreathRate entities in the database.
func (brcb *BreathRateCreateBulk) Save(ctx context.Context) ([]*BreathRate, error) {
	specs := make([]*sqlgraph.CreateSpec, len(brcb.builders))
	nodes := make([]*BreathRate, len(brcb.builders))
	mutators := make([]Mutator, len(brcb.builders))
	for i := range brcb.builders {
		func(i int, root context.Context) {
			builder := brcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*BreathRateMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, brcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, brcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, brcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (brcb *BreathRateCreateBulk) SaveX(ctx context.Context) []*BreathRate {
	v, err := brcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (brcb *BreathRateCreateBulk) Exec(ctx context.Context) error {
	_, err := brcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (brcb *BreathRateCreateBulk) ExecX(ctx context.Context) {
	if err := brcb.Exec(ctx); err != nil {
		panic(err)
	}
}

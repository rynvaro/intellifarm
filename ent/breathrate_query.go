// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/breathrate"
	"cattleai/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// BreathRateQuery is the builder for querying BreathRate entities.
type BreathRateQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.BreathRate
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BreathRateQuery builder.
func (brq *BreathRateQuery) Where(ps ...predicate.BreathRate) *BreathRateQuery {
	brq.predicates = append(brq.predicates, ps...)
	return brq
}

// Limit adds a limit step to the query.
func (brq *BreathRateQuery) Limit(limit int) *BreathRateQuery {
	brq.limit = &limit
	return brq
}

// Offset adds an offset step to the query.
func (brq *BreathRateQuery) Offset(offset int) *BreathRateQuery {
	brq.offset = &offset
	return brq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (brq *BreathRateQuery) Unique(unique bool) *BreathRateQuery {
	brq.unique = &unique
	return brq
}

// Order adds an order step to the query.
func (brq *BreathRateQuery) Order(o ...OrderFunc) *BreathRateQuery {
	brq.order = append(brq.order, o...)
	return brq
}

// First returns the first BreathRate entity from the query.
// Returns a *NotFoundError when no BreathRate was found.
func (brq *BreathRateQuery) First(ctx context.Context) (*BreathRate, error) {
	nodes, err := brq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{breathrate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (brq *BreathRateQuery) FirstX(ctx context.Context) *BreathRate {
	node, err := brq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BreathRate ID from the query.
// Returns a *NotFoundError when no BreathRate ID was found.
func (brq *BreathRateQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = brq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{breathrate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (brq *BreathRateQuery) FirstIDX(ctx context.Context) int {
	id, err := brq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BreathRate entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BreathRate entity is found.
// Returns a *NotFoundError when no BreathRate entities are found.
func (brq *BreathRateQuery) Only(ctx context.Context) (*BreathRate, error) {
	nodes, err := brq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{breathrate.Label}
	default:
		return nil, &NotSingularError{breathrate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (brq *BreathRateQuery) OnlyX(ctx context.Context) *BreathRate {
	node, err := brq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BreathRate ID in the query.
// Returns a *NotSingularError when more than one BreathRate ID is found.
// Returns a *NotFoundError when no entities are found.
func (brq *BreathRateQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = brq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{breathrate.Label}
	default:
		err = &NotSingularError{breathrate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (brq *BreathRateQuery) OnlyIDX(ctx context.Context) int {
	id, err := brq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BreathRates.
func (brq *BreathRateQuery) All(ctx context.Context) ([]*BreathRate, error) {
	if err := brq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return brq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (brq *BreathRateQuery) AllX(ctx context.Context) []*BreathRate {
	nodes, err := brq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BreathRate IDs.
func (brq *BreathRateQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := brq.Select(breathrate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (brq *BreathRateQuery) IDsX(ctx context.Context) []int {
	ids, err := brq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (brq *BreathRateQuery) Count(ctx context.Context) (int, error) {
	if err := brq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return brq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (brq *BreathRateQuery) CountX(ctx context.Context) int {
	count, err := brq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (brq *BreathRateQuery) Exist(ctx context.Context) (bool, error) {
	if err := brq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return brq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (brq *BreathRateQuery) ExistX(ctx context.Context) bool {
	exist, err := brq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BreathRateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (brq *BreathRateQuery) Clone() *BreathRateQuery {
	if brq == nil {
		return nil
	}
	return &BreathRateQuery{
		config:     brq.config,
		limit:      brq.limit,
		offset:     brq.offset,
		order:      append([]OrderFunc{}, brq.order...),
		predicates: append([]predicate.BreathRate{}, brq.predicates...),
		// clone intermediate query.
		sql:    brq.sql.Clone(),
		path:   brq.path,
		unique: brq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BreathRate.Query().
//		GroupBy(breathrate.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (brq *BreathRateQuery) GroupBy(field string, fields ...string) *BreathRateGroupBy {
	grbuild := &BreathRateGroupBy{config: brq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := brq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return brq.sqlQuery(ctx), nil
	}
	grbuild.label = breathrate.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.BreathRate.Query().
//		Select(breathrate.FieldName).
//		Scan(ctx, &v)
func (brq *BreathRateQuery) Select(fields ...string) *BreathRateSelect {
	brq.fields = append(brq.fields, fields...)
	selbuild := &BreathRateSelect{BreathRateQuery: brq}
	selbuild.label = breathrate.Label
	selbuild.flds, selbuild.scan = &brq.fields, selbuild.Scan
	return selbuild
}

func (brq *BreathRateQuery) prepareQuery(ctx context.Context) error {
	for _, f := range brq.fields {
		if !breathrate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if brq.path != nil {
		prev, err := brq.path(ctx)
		if err != nil {
			return err
		}
		brq.sql = prev
	}
	return nil
}

func (brq *BreathRateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BreathRate, error) {
	var (
		nodes = []*BreathRate{}
		_spec = brq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*BreathRate).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &BreathRate{config: brq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, brq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (brq *BreathRateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := brq.querySpec()
	_spec.Node.Columns = brq.fields
	if len(brq.fields) > 0 {
		_spec.Unique = brq.unique != nil && *brq.unique
	}
	return sqlgraph.CountNodes(ctx, brq.driver, _spec)
}

func (brq *BreathRateQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := brq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (brq *BreathRateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   breathrate.Table,
			Columns: breathrate.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: breathrate.FieldID,
			},
		},
		From:   brq.sql,
		Unique: true,
	}
	if unique := brq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := brq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, breathrate.FieldID)
		for i := range fields {
			if fields[i] != breathrate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := brq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := brq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := brq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := brq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (brq *BreathRateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(brq.driver.Dialect())
	t1 := builder.Table(breathrate.Table)
	columns := brq.fields
	if len(columns) == 0 {
		columns = breathrate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if brq.sql != nil {
		selector = brq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if brq.unique != nil && *brq.unique {
		selector.Distinct()
	}
	for _, p := range brq.predicates {
		p(selector)
	}
	for _, p := range brq.order {
		p(selector)
	}
	if offset := brq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := brq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BreathRateGroupBy is the group-by builder for BreathRate entities.
type BreathRateGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (brgb *BreathRateGroupBy) Aggregate(fns ...AggregateFunc) *BreathRateGroupBy {
	brgb.fns = append(brgb.fns, fns...)
	return brgb
}

// Scan applies the group-by query and scans the result into the given value.
func (brgb *BreathRateGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := brgb.path(ctx)
	if err != nil {
		return err
	}
	brgb.sql = query
	return brgb.sqlScan(ctx, v)
}

func (brgb *BreathRateGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range brgb.fields {
		if !breathrate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := brgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := brgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (brgb *BreathRateGroupBy) sqlQuery() *sql.Selector {
	selector := brgb.sql.Select()
	aggregation := make([]string, 0, len(brgb.fns))
	for _, fn := range brgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(brgb.fields)+len(brgb.fns))
		for _, f := range brgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(brgb.fields...)...)
}

// BreathRateSelect is the builder for selecting fields of BreathRate entities.
type BreathRateSelect struct {
	*BreathRateQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (brs *BreathRateSelect) Scan(ctx context.Context, v interface{}) error {
	if err := brs.prepareQuery(ctx); err != nil {
		return err
	}
	brs.sql = brs.BreathRateQuery.sqlQuery(ctx)
	return brs.sqlScan(ctx, v)
}

func (brs *BreathRateSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := brs.sql.Query()
	if err := brs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

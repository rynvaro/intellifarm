// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/calvecount"
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
)

// CalveCountCreate is the builder for creating a CalveCount entity.
type CalveCountCreate struct {
	config
	mutation *CalveCountMutation
	hooks    []Hook
}

// SetName sets the name field.
func (ccc *CalveCountCreate) SetName(s string) *CalveCountCreate {
	ccc.mutation.SetName(s)
	return ccc
}

// Mutation returns the CalveCountMutation object of the builder.
func (ccc *CalveCountCreate) Mutation() *CalveCountMutation {
	return ccc.mutation
}

// Save creates the CalveCount in the database.
func (ccc *CalveCountCreate) Save(ctx context.Context) (*CalveCount, error) {
	var (
		err  error
		node *CalveCount
	)
	if len(ccc.hooks) == 0 {
		if err = ccc.check(); err != nil {
			return nil, err
		}
		node, err = ccc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*CalveCountMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ccc.check(); err != nil {
				return nil, err
			}
			ccc.mutation = mutation
			node, err = ccc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(ccc.hooks) - 1; i >= 0; i-- {
			mut = ccc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ccc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (ccc *CalveCountCreate) SaveX(ctx context.Context) *CalveCount {
	v, err := ccc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (ccc *CalveCountCreate) check() error {
	if _, ok := ccc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := ccc.mutation.Name(); ok {
		if err := calvecount.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (ccc *CalveCountCreate) sqlSave(ctx context.Context) (*CalveCount, error) {
	_node, _spec := ccc.createSpec()
	if err := sqlgraph.CreateNode(ctx, ccc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int64(id)
	return _node, nil
}

func (ccc *CalveCountCreate) createSpec() (*CalveCount, *sqlgraph.CreateSpec) {
	var (
		_node = &CalveCount{config: ccc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: calvecount.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: calvecount.FieldID,
			},
		}
	)
	if value, ok := ccc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: calvecount.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// CalveCountCreateBulk is the builder for creating a bulk of CalveCount entities.
type CalveCountCreateBulk struct {
	config
	builders []*CalveCountCreate
}

// Save creates the CalveCount entities in the database.
func (cccb *CalveCountCreateBulk) Save(ctx context.Context) ([]*CalveCount, error) {
	specs := make([]*sqlgraph.CreateSpec, len(cccb.builders))
	nodes := make([]*CalveCount, len(cccb.builders))
	mutators := make([]Mutator, len(cccb.builders))
	for i := range cccb.builders {
		func(i int, root context.Context) {
			builder := cccb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*CalveCountMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, cccb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, cccb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int64(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, cccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (cccb *CalveCountCreateBulk) SaveX(ctx context.Context) []*CalveCount {
	v, err := cccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

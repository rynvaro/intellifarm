// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/calvetype"
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
)

// CalveTypeCreate is the builder for creating a CalveType entity.
type CalveTypeCreate struct {
	config
	mutation *CalveTypeMutation
	hooks    []Hook
}

// SetName sets the name field.
func (ctc *CalveTypeCreate) SetName(s string) *CalveTypeCreate {
	ctc.mutation.SetName(s)
	return ctc
}

// Mutation returns the CalveTypeMutation object of the builder.
func (ctc *CalveTypeCreate) Mutation() *CalveTypeMutation {
	return ctc.mutation
}

// Save creates the CalveType in the database.
func (ctc *CalveTypeCreate) Save(ctx context.Context) (*CalveType, error) {
	var (
		err  error
		node *CalveType
	)
	if len(ctc.hooks) == 0 {
		if err = ctc.check(); err != nil {
			return nil, err
		}
		node, err = ctc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*CalveTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ctc.check(); err != nil {
				return nil, err
			}
			ctc.mutation = mutation
			node, err = ctc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(ctc.hooks) - 1; i >= 0; i-- {
			mut = ctc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ctc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (ctc *CalveTypeCreate) SaveX(ctx context.Context) *CalveType {
	v, err := ctc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (ctc *CalveTypeCreate) check() error {
	if _, ok := ctc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := ctc.mutation.Name(); ok {
		if err := calvetype.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (ctc *CalveTypeCreate) sqlSave(ctx context.Context) (*CalveType, error) {
	_node, _spec := ctc.createSpec()
	if err := sqlgraph.CreateNode(ctx, ctc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int64(id)
	return _node, nil
}

func (ctc *CalveTypeCreate) createSpec() (*CalveType, *sqlgraph.CreateSpec) {
	var (
		_node = &CalveType{config: ctc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: calvetype.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: calvetype.FieldID,
			},
		}
	)
	if value, ok := ctc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: calvetype.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// CalveTypeCreateBulk is the builder for creating a bulk of CalveType entities.
type CalveTypeCreateBulk struct {
	config
	builders []*CalveTypeCreate
}

// Save creates the CalveType entities in the database.
func (ctcb *CalveTypeCreateBulk) Save(ctx context.Context) ([]*CalveType, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ctcb.builders))
	nodes := make([]*CalveType, len(ctcb.builders))
	mutators := make([]Mutator, len(ctcb.builders))
	for i := range ctcb.builders {
		func(i int, root context.Context) {
			builder := ctcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*CalveTypeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ctcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ctcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int64(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ctcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (ctcb *CalveTypeCreateBulk) SaveX(ctx context.Context) []*CalveType {
	v, err := ctcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

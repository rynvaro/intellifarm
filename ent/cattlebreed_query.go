// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/cattlebreed"
	"cattleai/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CattleBreedQuery is the builder for querying CattleBreed entities.
type CattleBreedQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.CattleBreed
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CattleBreedQuery builder.
func (cbq *CattleBreedQuery) Where(ps ...predicate.CattleBreed) *CattleBreedQuery {
	cbq.predicates = append(cbq.predicates, ps...)
	return cbq
}

// Limit adds a limit step to the query.
func (cbq *CattleBreedQuery) Limit(limit int) *CattleBreedQuery {
	cbq.limit = &limit
	return cbq
}

// Offset adds an offset step to the query.
func (cbq *CattleBreedQuery) Offset(offset int) *CattleBreedQuery {
	cbq.offset = &offset
	return cbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cbq *CattleBreedQuery) Unique(unique bool) *CattleBreedQuery {
	cbq.unique = &unique
	return cbq
}

// Order adds an order step to the query.
func (cbq *CattleBreedQuery) Order(o ...OrderFunc) *CattleBreedQuery {
	cbq.order = append(cbq.order, o...)
	return cbq
}

// First returns the first CattleBreed entity from the query.
// Returns a *NotFoundError when no CattleBreed was found.
func (cbq *CattleBreedQuery) First(ctx context.Context) (*CattleBreed, error) {
	nodes, err := cbq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{cattlebreed.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cbq *CattleBreedQuery) FirstX(ctx context.Context) *CattleBreed {
	node, err := cbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CattleBreed ID from the query.
// Returns a *NotFoundError when no CattleBreed ID was found.
func (cbq *CattleBreedQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cbq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{cattlebreed.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cbq *CattleBreedQuery) FirstIDX(ctx context.Context) int {
	id, err := cbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CattleBreed entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CattleBreed entity is found.
// Returns a *NotFoundError when no CattleBreed entities are found.
func (cbq *CattleBreedQuery) Only(ctx context.Context) (*CattleBreed, error) {
	nodes, err := cbq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{cattlebreed.Label}
	default:
		return nil, &NotSingularError{cattlebreed.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cbq *CattleBreedQuery) OnlyX(ctx context.Context) *CattleBreed {
	node, err := cbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CattleBreed ID in the query.
// Returns a *NotSingularError when more than one CattleBreed ID is found.
// Returns a *NotFoundError when no entities are found.
func (cbq *CattleBreedQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cbq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{cattlebreed.Label}
	default:
		err = &NotSingularError{cattlebreed.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cbq *CattleBreedQuery) OnlyIDX(ctx context.Context) int {
	id, err := cbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CattleBreeds.
func (cbq *CattleBreedQuery) All(ctx context.Context) ([]*CattleBreed, error) {
	if err := cbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return cbq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (cbq *CattleBreedQuery) AllX(ctx context.Context) []*CattleBreed {
	nodes, err := cbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CattleBreed IDs.
func (cbq *CattleBreedQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := cbq.Select(cattlebreed.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cbq *CattleBreedQuery) IDsX(ctx context.Context) []int {
	ids, err := cbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cbq *CattleBreedQuery) Count(ctx context.Context) (int, error) {
	if err := cbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return cbq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (cbq *CattleBreedQuery) CountX(ctx context.Context) int {
	count, err := cbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cbq *CattleBreedQuery) Exist(ctx context.Context) (bool, error) {
	if err := cbq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return cbq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (cbq *CattleBreedQuery) ExistX(ctx context.Context) bool {
	exist, err := cbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CattleBreedQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cbq *CattleBreedQuery) Clone() *CattleBreedQuery {
	if cbq == nil {
		return nil
	}
	return &CattleBreedQuery{
		config:     cbq.config,
		limit:      cbq.limit,
		offset:     cbq.offset,
		order:      append([]OrderFunc{}, cbq.order...),
		predicates: append([]predicate.CattleBreed{}, cbq.predicates...),
		// clone intermediate query.
		sql:    cbq.sql.Clone(),
		path:   cbq.path,
		unique: cbq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CattleBreed.Query().
//		GroupBy(cattlebreed.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cbq *CattleBreedQuery) GroupBy(field string, fields ...string) *CattleBreedGroupBy {
	grbuild := &CattleBreedGroupBy{config: cbq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := cbq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return cbq.sqlQuery(ctx), nil
	}
	grbuild.label = cattlebreed.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.CattleBreed.Query().
//		Select(cattlebreed.FieldName).
//		Scan(ctx, &v)
func (cbq *CattleBreedQuery) Select(fields ...string) *CattleBreedSelect {
	cbq.fields = append(cbq.fields, fields...)
	selbuild := &CattleBreedSelect{CattleBreedQuery: cbq}
	selbuild.label = cattlebreed.Label
	selbuild.flds, selbuild.scan = &cbq.fields, selbuild.Scan
	return selbuild
}

func (cbq *CattleBreedQuery) prepareQuery(ctx context.Context) error {
	for _, f := range cbq.fields {
		if !cattlebreed.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cbq.path != nil {
		prev, err := cbq.path(ctx)
		if err != nil {
			return err
		}
		cbq.sql = prev
	}
	return nil
}

func (cbq *CattleBreedQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CattleBreed, error) {
	var (
		nodes = []*CattleBreed{}
		_spec = cbq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*CattleBreed).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &CattleBreed{config: cbq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cbq *CattleBreedQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cbq.querySpec()
	_spec.Node.Columns = cbq.fields
	if len(cbq.fields) > 0 {
		_spec.Unique = cbq.unique != nil && *cbq.unique
	}
	return sqlgraph.CountNodes(ctx, cbq.driver, _spec)
}

func (cbq *CattleBreedQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := cbq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (cbq *CattleBreedQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   cattlebreed.Table,
			Columns: cattlebreed.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: cattlebreed.FieldID,
			},
		},
		From:   cbq.sql,
		Unique: true,
	}
	if unique := cbq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := cbq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, cattlebreed.FieldID)
		for i := range fields {
			if fields[i] != cattlebreed.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cbq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cbq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cbq *CattleBreedQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cbq.driver.Dialect())
	t1 := builder.Table(cattlebreed.Table)
	columns := cbq.fields
	if len(columns) == 0 {
		columns = cattlebreed.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cbq.sql != nil {
		selector = cbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cbq.unique != nil && *cbq.unique {
		selector.Distinct()
	}
	for _, p := range cbq.predicates {
		p(selector)
	}
	for _, p := range cbq.order {
		p(selector)
	}
	if offset := cbq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cbq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CattleBreedGroupBy is the group-by builder for CattleBreed entities.
type CattleBreedGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cbgb *CattleBreedGroupBy) Aggregate(fns ...AggregateFunc) *CattleBreedGroupBy {
	cbgb.fns = append(cbgb.fns, fns...)
	return cbgb
}

// Scan applies the group-by query and scans the result into the given value.
func (cbgb *CattleBreedGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := cbgb.path(ctx)
	if err != nil {
		return err
	}
	cbgb.sql = query
	return cbgb.sqlScan(ctx, v)
}

func (cbgb *CattleBreedGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range cbgb.fields {
		if !cattlebreed.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := cbgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cbgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cbgb *CattleBreedGroupBy) sqlQuery() *sql.Selector {
	selector := cbgb.sql.Select()
	aggregation := make([]string, 0, len(cbgb.fns))
	for _, fn := range cbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(cbgb.fields)+len(cbgb.fns))
		for _, f := range cbgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(cbgb.fields...)...)
}

// CattleBreedSelect is the builder for selecting fields of CattleBreed entities.
type CattleBreedSelect struct {
	*CattleBreedQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (cbs *CattleBreedSelect) Scan(ctx context.Context, v interface{}) error {
	if err := cbs.prepareQuery(ctx); err != nil {
		return err
	}
	cbs.sql = cbs.CattleBreedQuery.sqlQuery(ctx)
	return cbs.sqlScan(ctx, v)
}

func (cbs *CattleBreedSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := cbs.sql.Query()
	if err := cbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

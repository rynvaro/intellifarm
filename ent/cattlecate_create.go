// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/cattlecate"
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
)

// CattleCateCreate is the builder for creating a CattleCate entity.
type CattleCateCreate struct {
	config
	mutation *CattleCateMutation
	hooks    []Hook
}

// SetName sets the name field.
func (ccc *CattleCateCreate) SetName(s string) *CattleCateCreate {
	ccc.mutation.SetName(s)
	return ccc
}

// SetGenderIds sets the genderIds field.
func (ccc *CattleCateCreate) SetGenderIds(s string) *CattleCateCreate {
	ccc.mutation.SetGenderIds(s)
	return ccc
}

// Mutation returns the CattleCateMutation object of the builder.
func (ccc *CattleCateCreate) Mutation() *CattleCateMutation {
	return ccc.mutation
}

// Save creates the CattleCate in the database.
func (ccc *CattleCateCreate) Save(ctx context.Context) (*CattleCate, error) {
	var (
		err  error
		node *CattleCate
	)
	if len(ccc.hooks) == 0 {
		if err = ccc.check(); err != nil {
			return nil, err
		}
		node, err = ccc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*CattleCateMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ccc.check(); err != nil {
				return nil, err
			}
			ccc.mutation = mutation
			node, err = ccc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(ccc.hooks) - 1; i >= 0; i-- {
			mut = ccc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ccc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (ccc *CattleCateCreate) SaveX(ctx context.Context) *CattleCate {
	v, err := ccc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (ccc *CattleCateCreate) check() error {
	if _, ok := ccc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := ccc.mutation.Name(); ok {
		if err := cattlecate.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	if _, ok := ccc.mutation.GenderIds(); !ok {
		return &ValidationError{Name: "genderIds", err: errors.New("ent: missing required field \"genderIds\"")}
	}
	if v, ok := ccc.mutation.GenderIds(); ok {
		if err := cattlecate.GenderIdsValidator(v); err != nil {
			return &ValidationError{Name: "genderIds", err: fmt.Errorf("ent: validator failed for field \"genderIds\": %w", err)}
		}
	}
	return nil
}

func (ccc *CattleCateCreate) sqlSave(ctx context.Context) (*CattleCate, error) {
	_node, _spec := ccc.createSpec()
	if err := sqlgraph.CreateNode(ctx, ccc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int64(id)
	return _node, nil
}

func (ccc *CattleCateCreate) createSpec() (*CattleCate, *sqlgraph.CreateSpec) {
	var (
		_node = &CattleCate{config: ccc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: cattlecate.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: cattlecate.FieldID,
			},
		}
	)
	if value, ok := ccc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: cattlecate.FieldName,
		})
		_node.Name = value
	}
	if value, ok := ccc.mutation.GenderIds(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: cattlecate.FieldGenderIds,
		})
		_node.GenderIds = value
	}
	return _node, _spec
}

// CattleCateCreateBulk is the builder for creating a bulk of CattleCate entities.
type CattleCateCreateBulk struct {
	config
	builders []*CattleCateCreate
}

// Save creates the CattleCate entities in the database.
func (cccb *CattleCateCreateBulk) Save(ctx context.Context) ([]*CattleCate, error) {
	specs := make([]*sqlgraph.CreateSpec, len(cccb.builders))
	nodes := make([]*CattleCate, len(cccb.builders))
	mutators := make([]Mutator, len(cccb.builders))
	for i := range cccb.builders {
		func(i int, root context.Context) {
			builder := cccb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*CattleCateMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, cccb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, cccb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int64(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, cccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (cccb *CattleCateCreateBulk) SaveX(ctx context.Context) []*CattleCate {
	v, err := cccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

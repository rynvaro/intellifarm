// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/cattledie"
	"cattleai/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CattleDieQuery is the builder for querying CattleDie entities.
type CattleDieQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.CattleDie
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CattleDieQuery builder.
func (cdq *CattleDieQuery) Where(ps ...predicate.CattleDie) *CattleDieQuery {
	cdq.predicates = append(cdq.predicates, ps...)
	return cdq
}

// Limit adds a limit step to the query.
func (cdq *CattleDieQuery) Limit(limit int) *CattleDieQuery {
	cdq.limit = &limit
	return cdq
}

// Offset adds an offset step to the query.
func (cdq *CattleDieQuery) Offset(offset int) *CattleDieQuery {
	cdq.offset = &offset
	return cdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cdq *CattleDieQuery) Unique(unique bool) *CattleDieQuery {
	cdq.unique = &unique
	return cdq
}

// Order adds an order step to the query.
func (cdq *CattleDieQuery) Order(o ...OrderFunc) *CattleDieQuery {
	cdq.order = append(cdq.order, o...)
	return cdq
}

// First returns the first CattleDie entity from the query.
// Returns a *NotFoundError when no CattleDie was found.
func (cdq *CattleDieQuery) First(ctx context.Context) (*CattleDie, error) {
	nodes, err := cdq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{cattledie.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cdq *CattleDieQuery) FirstX(ctx context.Context) *CattleDie {
	node, err := cdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CattleDie ID from the query.
// Returns a *NotFoundError when no CattleDie ID was found.
func (cdq *CattleDieQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cdq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{cattledie.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cdq *CattleDieQuery) FirstIDX(ctx context.Context) int {
	id, err := cdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CattleDie entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CattleDie entity is found.
// Returns a *NotFoundError when no CattleDie entities are found.
func (cdq *CattleDieQuery) Only(ctx context.Context) (*CattleDie, error) {
	nodes, err := cdq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{cattledie.Label}
	default:
		return nil, &NotSingularError{cattledie.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cdq *CattleDieQuery) OnlyX(ctx context.Context) *CattleDie {
	node, err := cdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CattleDie ID in the query.
// Returns a *NotSingularError when more than one CattleDie ID is found.
// Returns a *NotFoundError when no entities are found.
func (cdq *CattleDieQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cdq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{cattledie.Label}
	default:
		err = &NotSingularError{cattledie.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cdq *CattleDieQuery) OnlyIDX(ctx context.Context) int {
	id, err := cdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CattleDies.
func (cdq *CattleDieQuery) All(ctx context.Context) ([]*CattleDie, error) {
	if err := cdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return cdq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (cdq *CattleDieQuery) AllX(ctx context.Context) []*CattleDie {
	nodes, err := cdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CattleDie IDs.
func (cdq *CattleDieQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := cdq.Select(cattledie.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cdq *CattleDieQuery) IDsX(ctx context.Context) []int {
	ids, err := cdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cdq *CattleDieQuery) Count(ctx context.Context) (int, error) {
	if err := cdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return cdq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (cdq *CattleDieQuery) CountX(ctx context.Context) int {
	count, err := cdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cdq *CattleDieQuery) Exist(ctx context.Context) (bool, error) {
	if err := cdq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return cdq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (cdq *CattleDieQuery) ExistX(ctx context.Context) bool {
	exist, err := cdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CattleDieQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cdq *CattleDieQuery) Clone() *CattleDieQuery {
	if cdq == nil {
		return nil
	}
	return &CattleDieQuery{
		config:     cdq.config,
		limit:      cdq.limit,
		offset:     cdq.offset,
		order:      append([]OrderFunc{}, cdq.order...),
		predicates: append([]predicate.CattleDie{}, cdq.predicates...),
		// clone intermediate query.
		sql:    cdq.sql.Clone(),
		path:   cdq.path,
		unique: cdq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CattleId int64 `json:"cattleId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CattleDie.Query().
//		GroupBy(cattledie.FieldCattleId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cdq *CattleDieQuery) GroupBy(field string, fields ...string) *CattleDieGroupBy {
	grbuild := &CattleDieGroupBy{config: cdq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := cdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return cdq.sqlQuery(ctx), nil
	}
	grbuild.label = cattledie.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CattleId int64 `json:"cattleId,omitempty"`
//	}
//
//	client.CattleDie.Query().
//		Select(cattledie.FieldCattleId).
//		Scan(ctx, &v)
func (cdq *CattleDieQuery) Select(fields ...string) *CattleDieSelect {
	cdq.fields = append(cdq.fields, fields...)
	selbuild := &CattleDieSelect{CattleDieQuery: cdq}
	selbuild.label = cattledie.Label
	selbuild.flds, selbuild.scan = &cdq.fields, selbuild.Scan
	return selbuild
}

func (cdq *CattleDieQuery) prepareQuery(ctx context.Context) error {
	for _, f := range cdq.fields {
		if !cattledie.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cdq.path != nil {
		prev, err := cdq.path(ctx)
		if err != nil {
			return err
		}
		cdq.sql = prev
	}
	return nil
}

func (cdq *CattleDieQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CattleDie, error) {
	var (
		nodes = []*CattleDie{}
		_spec = cdq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*CattleDie).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &CattleDie{config: cdq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cdq *CattleDieQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cdq.querySpec()
	_spec.Node.Columns = cdq.fields
	if len(cdq.fields) > 0 {
		_spec.Unique = cdq.unique != nil && *cdq.unique
	}
	return sqlgraph.CountNodes(ctx, cdq.driver, _spec)
}

func (cdq *CattleDieQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := cdq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (cdq *CattleDieQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   cattledie.Table,
			Columns: cattledie.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: cattledie.FieldID,
			},
		},
		From:   cdq.sql,
		Unique: true,
	}
	if unique := cdq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := cdq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, cattledie.FieldID)
		for i := range fields {
			if fields[i] != cattledie.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cdq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cdq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cdq *CattleDieQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cdq.driver.Dialect())
	t1 := builder.Table(cattledie.Table)
	columns := cdq.fields
	if len(columns) == 0 {
		columns = cattledie.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cdq.sql != nil {
		selector = cdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cdq.unique != nil && *cdq.unique {
		selector.Distinct()
	}
	for _, p := range cdq.predicates {
		p(selector)
	}
	for _, p := range cdq.order {
		p(selector)
	}
	if offset := cdq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cdq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CattleDieGroupBy is the group-by builder for CattleDie entities.
type CattleDieGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cdgb *CattleDieGroupBy) Aggregate(fns ...AggregateFunc) *CattleDieGroupBy {
	cdgb.fns = append(cdgb.fns, fns...)
	return cdgb
}

// Scan applies the group-by query and scans the result into the given value.
func (cdgb *CattleDieGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := cdgb.path(ctx)
	if err != nil {
		return err
	}
	cdgb.sql = query
	return cdgb.sqlScan(ctx, v)
}

func (cdgb *CattleDieGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range cdgb.fields {
		if !cattledie.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := cdgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cdgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cdgb *CattleDieGroupBy) sqlQuery() *sql.Selector {
	selector := cdgb.sql.Select()
	aggregation := make([]string, 0, len(cdgb.fns))
	for _, fn := range cdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(cdgb.fields)+len(cdgb.fns))
		for _, f := range cdgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(cdgb.fields...)...)
}

// CattleDieSelect is the builder for selecting fields of CattleDie entities.
type CattleDieSelect struct {
	*CattleDieQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (cds *CattleDieSelect) Scan(ctx context.Context, v interface{}) error {
	if err := cds.prepareQuery(ctx); err != nil {
		return err
	}
	cds.sql = cds.CattleDieQuery.sqlQuery(ctx)
	return cds.sqlScan(ctx, v)
}

func (cds *CattleDieSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := cds.sql.Query()
	if err := cds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

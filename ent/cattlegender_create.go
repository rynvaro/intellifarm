// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/cattlegender"
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
)

// CattleGenderCreate is the builder for creating a CattleGender entity.
type CattleGenderCreate struct {
	config
	mutation *CattleGenderMutation
	hooks    []Hook
}

// SetName sets the name field.
func (cgc *CattleGenderCreate) SetName(s string) *CattleGenderCreate {
	cgc.mutation.SetName(s)
	return cgc
}

// Mutation returns the CattleGenderMutation object of the builder.
func (cgc *CattleGenderCreate) Mutation() *CattleGenderMutation {
	return cgc.mutation
}

// Save creates the CattleGender in the database.
func (cgc *CattleGenderCreate) Save(ctx context.Context) (*CattleGender, error) {
	var (
		err  error
		node *CattleGender
	)
	if len(cgc.hooks) == 0 {
		if err = cgc.check(); err != nil {
			return nil, err
		}
		node, err = cgc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*CattleGenderMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = cgc.check(); err != nil {
				return nil, err
			}
			cgc.mutation = mutation
			node, err = cgc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(cgc.hooks) - 1; i >= 0; i-- {
			mut = cgc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, cgc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (cgc *CattleGenderCreate) SaveX(ctx context.Context) *CattleGender {
	v, err := cgc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (cgc *CattleGenderCreate) check() error {
	if _, ok := cgc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := cgc.mutation.Name(); ok {
		if err := cattlegender.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (cgc *CattleGenderCreate) sqlSave(ctx context.Context) (*CattleGender, error) {
	_node, _spec := cgc.createSpec()
	if err := sqlgraph.CreateNode(ctx, cgc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int64(id)
	return _node, nil
}

func (cgc *CattleGenderCreate) createSpec() (*CattleGender, *sqlgraph.CreateSpec) {
	var (
		_node = &CattleGender{config: cgc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: cattlegender.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: cattlegender.FieldID,
			},
		}
	)
	if value, ok := cgc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: cattlegender.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// CattleGenderCreateBulk is the builder for creating a bulk of CattleGender entities.
type CattleGenderCreateBulk struct {
	config
	builders []*CattleGenderCreate
}

// Save creates the CattleGender entities in the database.
func (cgcb *CattleGenderCreateBulk) Save(ctx context.Context) ([]*CattleGender, error) {
	specs := make([]*sqlgraph.CreateSpec, len(cgcb.builders))
	nodes := make([]*CattleGender, len(cgcb.builders))
	mutators := make([]Mutator, len(cgcb.builders))
	for i := range cgcb.builders {
		func(i int, root context.Context) {
			builder := cgcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*CattleGenderMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, cgcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, cgcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int64(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, cgcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (cgcb *CattleGenderCreateBulk) SaveX(ctx context.Context) []*CattleGender {
	v, err := cgcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

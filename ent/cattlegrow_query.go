// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/cattlegrow"
	"cattleai/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CattleGrowQuery is the builder for querying CattleGrow entities.
type CattleGrowQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.CattleGrow
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CattleGrowQuery builder.
func (cgq *CattleGrowQuery) Where(ps ...predicate.CattleGrow) *CattleGrowQuery {
	cgq.predicates = append(cgq.predicates, ps...)
	return cgq
}

// Limit adds a limit step to the query.
func (cgq *CattleGrowQuery) Limit(limit int) *CattleGrowQuery {
	cgq.limit = &limit
	return cgq
}

// Offset adds an offset step to the query.
func (cgq *CattleGrowQuery) Offset(offset int) *CattleGrowQuery {
	cgq.offset = &offset
	return cgq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cgq *CattleGrowQuery) Unique(unique bool) *CattleGrowQuery {
	cgq.unique = &unique
	return cgq
}

// Order adds an order step to the query.
func (cgq *CattleGrowQuery) Order(o ...OrderFunc) *CattleGrowQuery {
	cgq.order = append(cgq.order, o...)
	return cgq
}

// First returns the first CattleGrow entity from the query.
// Returns a *NotFoundError when no CattleGrow was found.
func (cgq *CattleGrowQuery) First(ctx context.Context) (*CattleGrow, error) {
	nodes, err := cgq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{cattlegrow.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cgq *CattleGrowQuery) FirstX(ctx context.Context) *CattleGrow {
	node, err := cgq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CattleGrow ID from the query.
// Returns a *NotFoundError when no CattleGrow ID was found.
func (cgq *CattleGrowQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cgq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{cattlegrow.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cgq *CattleGrowQuery) FirstIDX(ctx context.Context) int {
	id, err := cgq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CattleGrow entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CattleGrow entity is found.
// Returns a *NotFoundError when no CattleGrow entities are found.
func (cgq *CattleGrowQuery) Only(ctx context.Context) (*CattleGrow, error) {
	nodes, err := cgq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{cattlegrow.Label}
	default:
		return nil, &NotSingularError{cattlegrow.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cgq *CattleGrowQuery) OnlyX(ctx context.Context) *CattleGrow {
	node, err := cgq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CattleGrow ID in the query.
// Returns a *NotSingularError when more than one CattleGrow ID is found.
// Returns a *NotFoundError when no entities are found.
func (cgq *CattleGrowQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cgq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{cattlegrow.Label}
	default:
		err = &NotSingularError{cattlegrow.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cgq *CattleGrowQuery) OnlyIDX(ctx context.Context) int {
	id, err := cgq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CattleGrows.
func (cgq *CattleGrowQuery) All(ctx context.Context) ([]*CattleGrow, error) {
	if err := cgq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return cgq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (cgq *CattleGrowQuery) AllX(ctx context.Context) []*CattleGrow {
	nodes, err := cgq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CattleGrow IDs.
func (cgq *CattleGrowQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := cgq.Select(cattlegrow.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cgq *CattleGrowQuery) IDsX(ctx context.Context) []int {
	ids, err := cgq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cgq *CattleGrowQuery) Count(ctx context.Context) (int, error) {
	if err := cgq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return cgq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (cgq *CattleGrowQuery) CountX(ctx context.Context) int {
	count, err := cgq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cgq *CattleGrowQuery) Exist(ctx context.Context) (bool, error) {
	if err := cgq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return cgq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (cgq *CattleGrowQuery) ExistX(ctx context.Context) bool {
	exist, err := cgq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CattleGrowQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cgq *CattleGrowQuery) Clone() *CattleGrowQuery {
	if cgq == nil {
		return nil
	}
	return &CattleGrowQuery{
		config:     cgq.config,
		limit:      cgq.limit,
		offset:     cgq.offset,
		order:      append([]OrderFunc{}, cgq.order...),
		predicates: append([]predicate.CattleGrow{}, cgq.predicates...),
		// clone intermediate query.
		sql:    cgq.sql.Clone(),
		path:   cgq.path,
		unique: cgq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CattleGrow.Query().
//		GroupBy(cattlegrow.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cgq *CattleGrowQuery) GroupBy(field string, fields ...string) *CattleGrowGroupBy {
	grbuild := &CattleGrowGroupBy{config: cgq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := cgq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return cgq.sqlQuery(ctx), nil
	}
	grbuild.label = cattlegrow.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.CattleGrow.Query().
//		Select(cattlegrow.FieldName).
//		Scan(ctx, &v)
func (cgq *CattleGrowQuery) Select(fields ...string) *CattleGrowSelect {
	cgq.fields = append(cgq.fields, fields...)
	selbuild := &CattleGrowSelect{CattleGrowQuery: cgq}
	selbuild.label = cattlegrow.Label
	selbuild.flds, selbuild.scan = &cgq.fields, selbuild.Scan
	return selbuild
}

func (cgq *CattleGrowQuery) prepareQuery(ctx context.Context) error {
	for _, f := range cgq.fields {
		if !cattlegrow.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cgq.path != nil {
		prev, err := cgq.path(ctx)
		if err != nil {
			return err
		}
		cgq.sql = prev
	}
	return nil
}

func (cgq *CattleGrowQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CattleGrow, error) {
	var (
		nodes = []*CattleGrow{}
		_spec = cgq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*CattleGrow).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &CattleGrow{config: cgq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cgq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cgq *CattleGrowQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cgq.querySpec()
	_spec.Node.Columns = cgq.fields
	if len(cgq.fields) > 0 {
		_spec.Unique = cgq.unique != nil && *cgq.unique
	}
	return sqlgraph.CountNodes(ctx, cgq.driver, _spec)
}

func (cgq *CattleGrowQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := cgq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (cgq *CattleGrowQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   cattlegrow.Table,
			Columns: cattlegrow.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: cattlegrow.FieldID,
			},
		},
		From:   cgq.sql,
		Unique: true,
	}
	if unique := cgq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := cgq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, cattlegrow.FieldID)
		for i := range fields {
			if fields[i] != cattlegrow.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cgq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cgq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cgq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cgq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cgq *CattleGrowQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cgq.driver.Dialect())
	t1 := builder.Table(cattlegrow.Table)
	columns := cgq.fields
	if len(columns) == 0 {
		columns = cattlegrow.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cgq.sql != nil {
		selector = cgq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cgq.unique != nil && *cgq.unique {
		selector.Distinct()
	}
	for _, p := range cgq.predicates {
		p(selector)
	}
	for _, p := range cgq.order {
		p(selector)
	}
	if offset := cgq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cgq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CattleGrowGroupBy is the group-by builder for CattleGrow entities.
type CattleGrowGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cggb *CattleGrowGroupBy) Aggregate(fns ...AggregateFunc) *CattleGrowGroupBy {
	cggb.fns = append(cggb.fns, fns...)
	return cggb
}

// Scan applies the group-by query and scans the result into the given value.
func (cggb *CattleGrowGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := cggb.path(ctx)
	if err != nil {
		return err
	}
	cggb.sql = query
	return cggb.sqlScan(ctx, v)
}

func (cggb *CattleGrowGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range cggb.fields {
		if !cattlegrow.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := cggb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cggb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cggb *CattleGrowGroupBy) sqlQuery() *sql.Selector {
	selector := cggb.sql.Select()
	aggregation := make([]string, 0, len(cggb.fns))
	for _, fn := range cggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(cggb.fields)+len(cggb.fns))
		for _, f := range cggb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(cggb.fields...)...)
}

// CattleGrowSelect is the builder for selecting fields of CattleGrow entities.
type CattleGrowSelect struct {
	*CattleGrowQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (cgs *CattleGrowSelect) Scan(ctx context.Context, v interface{}) error {
	if err := cgs.prepareQuery(ctx); err != nil {
		return err
	}
	cgs.sql = cgs.CattleGrowQuery.sqlQuery(ctx)
	return cgs.sqlScan(ctx, v)
}

func (cgs *CattleGrowSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := cgs.sql.Query()
	if err := cgs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/cattlehaircolor"
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// CattleHairColor is the model entity for the CattleHairColor schema.
type CattleHairColor struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 名称
	Name string `json:"name,omitempty"`
	// 代码
	Code string `json:"code,omitempty"`
	// 租户ID
	TenantId int64 `json:"tenantId,omitempty"`
	// 租户组织名称
	TenantName string `json:"tenantName,omitempty"`
	// 排序数字
	Order int `json:"order,omitempty"`
	// 备注
	Remarks string `json:"remarks,omitempty"`
	// 创建时间
	CreatedAt int64 `json:"createdAt,omitempty"`
	// 更新时间
	UpdatedAt int64 `json:"updatedAt,omitempty"`
	// 是否已删除
	Deleted int `json:"deleted,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CattleHairColor) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case cattlehaircolor.FieldID, cattlehaircolor.FieldTenantId, cattlehaircolor.FieldOrder, cattlehaircolor.FieldCreatedAt, cattlehaircolor.FieldUpdatedAt, cattlehaircolor.FieldDeleted:
			values[i] = new(sql.NullInt64)
		case cattlehaircolor.FieldName, cattlehaircolor.FieldCode, cattlehaircolor.FieldTenantName, cattlehaircolor.FieldRemarks:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type CattleHairColor", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CattleHairColor fields.
func (chc *CattleHairColor) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case cattlehaircolor.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			chc.ID = int(value.Int64)
		case cattlehaircolor.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				chc.Name = value.String
			}
		case cattlehaircolor.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				chc.Code = value.String
			}
		case cattlehaircolor.FieldTenantId:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field tenantId", values[i])
			} else if value.Valid {
				chc.TenantId = value.Int64
			}
		case cattlehaircolor.FieldTenantName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tenantName", values[i])
			} else if value.Valid {
				chc.TenantName = value.String
			}
		case cattlehaircolor.FieldOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field order", values[i])
			} else if value.Valid {
				chc.Order = int(value.Int64)
			}
		case cattlehaircolor.FieldRemarks:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field remarks", values[i])
			} else if value.Valid {
				chc.Remarks = value.String
			}
		case cattlehaircolor.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field createdAt", values[i])
			} else if value.Valid {
				chc.CreatedAt = value.Int64
			}
		case cattlehaircolor.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field updatedAt", values[i])
			} else if value.Valid {
				chc.UpdatedAt = value.Int64
			}
		case cattlehaircolor.FieldDeleted:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field deleted", values[i])
			} else if value.Valid {
				chc.Deleted = int(value.Int64)
			}
		}
	}
	return nil
}

// Update returns a builder for updating this CattleHairColor.
// Note that you need to call CattleHairColor.Unwrap() before calling this method if this CattleHairColor
// was returned from a transaction, and the transaction was committed or rolled back.
func (chc *CattleHairColor) Update() *CattleHairColorUpdateOne {
	return (&CattleHairColorClient{config: chc.config}).UpdateOne(chc)
}

// Unwrap unwraps the CattleHairColor entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (chc *CattleHairColor) Unwrap() *CattleHairColor {
	_tx, ok := chc.config.driver.(*txDriver)
	if !ok {
		panic("ent: CattleHairColor is not a transactional entity")
	}
	chc.config.driver = _tx.drv
	return chc
}

// String implements the fmt.Stringer.
func (chc *CattleHairColor) String() string {
	var builder strings.Builder
	builder.WriteString("CattleHairColor(")
	builder.WriteString(fmt.Sprintf("id=%v, ", chc.ID))
	builder.WriteString("name=")
	builder.WriteString(chc.Name)
	builder.WriteString(", ")
	builder.WriteString("code=")
	builder.WriteString(chc.Code)
	builder.WriteString(", ")
	builder.WriteString("tenantId=")
	builder.WriteString(fmt.Sprintf("%v", chc.TenantId))
	builder.WriteString(", ")
	builder.WriteString("tenantName=")
	builder.WriteString(chc.TenantName)
	builder.WriteString(", ")
	builder.WriteString("order=")
	builder.WriteString(fmt.Sprintf("%v", chc.Order))
	builder.WriteString(", ")
	builder.WriteString("remarks=")
	builder.WriteString(chc.Remarks)
	builder.WriteString(", ")
	builder.WriteString("createdAt=")
	builder.WriteString(fmt.Sprintf("%v", chc.CreatedAt))
	builder.WriteString(", ")
	builder.WriteString("updatedAt=")
	builder.WriteString(fmt.Sprintf("%v", chc.UpdatedAt))
	builder.WriteString(", ")
	builder.WriteString("deleted=")
	builder.WriteString(fmt.Sprintf("%v", chc.Deleted))
	builder.WriteByte(')')
	return builder.String()
}

// CattleHairColors is a parsable slice of CattleHairColor.
type CattleHairColors []*CattleHairColor

func (chc CattleHairColors) config(cfg config) {
	for _i := range chc {
		chc[_i].config = cfg
	}
}

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/cattlehaircolor"
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
)

// CattleHairColorCreate is the builder for creating a CattleHairColor entity.
type CattleHairColorCreate struct {
	config
	mutation *CattleHairColorMutation
	hooks    []Hook
}

// SetName sets the name field.
func (chcc *CattleHairColorCreate) SetName(s string) *CattleHairColorCreate {
	chcc.mutation.SetName(s)
	return chcc
}

// Mutation returns the CattleHairColorMutation object of the builder.
func (chcc *CattleHairColorCreate) Mutation() *CattleHairColorMutation {
	return chcc.mutation
}

// Save creates the CattleHairColor in the database.
func (chcc *CattleHairColorCreate) Save(ctx context.Context) (*CattleHairColor, error) {
	var (
		err  error
		node *CattleHairColor
	)
	if len(chcc.hooks) == 0 {
		if err = chcc.check(); err != nil {
			return nil, err
		}
		node, err = chcc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*CattleHairColorMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = chcc.check(); err != nil {
				return nil, err
			}
			chcc.mutation = mutation
			node, err = chcc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(chcc.hooks) - 1; i >= 0; i-- {
			mut = chcc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, chcc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (chcc *CattleHairColorCreate) SaveX(ctx context.Context) *CattleHairColor {
	v, err := chcc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (chcc *CattleHairColorCreate) check() error {
	if _, ok := chcc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := chcc.mutation.Name(); ok {
		if err := cattlehaircolor.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (chcc *CattleHairColorCreate) sqlSave(ctx context.Context) (*CattleHairColor, error) {
	_node, _spec := chcc.createSpec()
	if err := sqlgraph.CreateNode(ctx, chcc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int64(id)
	return _node, nil
}

func (chcc *CattleHairColorCreate) createSpec() (*CattleHairColor, *sqlgraph.CreateSpec) {
	var (
		_node = &CattleHairColor{config: chcc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: cattlehaircolor.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: cattlehaircolor.FieldID,
			},
		}
	)
	if value, ok := chcc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: cattlehaircolor.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// CattleHairColorCreateBulk is the builder for creating a bulk of CattleHairColor entities.
type CattleHairColorCreateBulk struct {
	config
	builders []*CattleHairColorCreate
}

// Save creates the CattleHairColor entities in the database.
func (chccb *CattleHairColorCreateBulk) Save(ctx context.Context) ([]*CattleHairColor, error) {
	specs := make([]*sqlgraph.CreateSpec, len(chccb.builders))
	nodes := make([]*CattleHairColor, len(chccb.builders))
	mutators := make([]Mutator, len(chccb.builders))
	for i := range chccb.builders {
		func(i int, root context.Context) {
			builder := chccb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*CattleHairColorMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, chccb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, chccb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int64(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, chccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (chccb *CattleHairColorCreateBulk) SaveX(ctx context.Context) []*CattleHairColor {
	v, err := chccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/cattleout"
	"cattleai/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CattleOutQuery is the builder for querying CattleOut entities.
type CattleOutQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.CattleOut
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CattleOutQuery builder.
func (coq *CattleOutQuery) Where(ps ...predicate.CattleOut) *CattleOutQuery {
	coq.predicates = append(coq.predicates, ps...)
	return coq
}

// Limit adds a limit step to the query.
func (coq *CattleOutQuery) Limit(limit int) *CattleOutQuery {
	coq.limit = &limit
	return coq
}

// Offset adds an offset step to the query.
func (coq *CattleOutQuery) Offset(offset int) *CattleOutQuery {
	coq.offset = &offset
	return coq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (coq *CattleOutQuery) Unique(unique bool) *CattleOutQuery {
	coq.unique = &unique
	return coq
}

// Order adds an order step to the query.
func (coq *CattleOutQuery) Order(o ...OrderFunc) *CattleOutQuery {
	coq.order = append(coq.order, o...)
	return coq
}

// First returns the first CattleOut entity from the query.
// Returns a *NotFoundError when no CattleOut was found.
func (coq *CattleOutQuery) First(ctx context.Context) (*CattleOut, error) {
	nodes, err := coq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{cattleout.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (coq *CattleOutQuery) FirstX(ctx context.Context) *CattleOut {
	node, err := coq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CattleOut ID from the query.
// Returns a *NotFoundError when no CattleOut ID was found.
func (coq *CattleOutQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = coq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{cattleout.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (coq *CattleOutQuery) FirstIDX(ctx context.Context) int {
	id, err := coq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CattleOut entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CattleOut entity is found.
// Returns a *NotFoundError when no CattleOut entities are found.
func (coq *CattleOutQuery) Only(ctx context.Context) (*CattleOut, error) {
	nodes, err := coq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{cattleout.Label}
	default:
		return nil, &NotSingularError{cattleout.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (coq *CattleOutQuery) OnlyX(ctx context.Context) *CattleOut {
	node, err := coq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CattleOut ID in the query.
// Returns a *NotSingularError when more than one CattleOut ID is found.
// Returns a *NotFoundError when no entities are found.
func (coq *CattleOutQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = coq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{cattleout.Label}
	default:
		err = &NotSingularError{cattleout.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (coq *CattleOutQuery) OnlyIDX(ctx context.Context) int {
	id, err := coq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CattleOuts.
func (coq *CattleOutQuery) All(ctx context.Context) ([]*CattleOut, error) {
	if err := coq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return coq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (coq *CattleOutQuery) AllX(ctx context.Context) []*CattleOut {
	nodes, err := coq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CattleOut IDs.
func (coq *CattleOutQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := coq.Select(cattleout.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (coq *CattleOutQuery) IDsX(ctx context.Context) []int {
	ids, err := coq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (coq *CattleOutQuery) Count(ctx context.Context) (int, error) {
	if err := coq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return coq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (coq *CattleOutQuery) CountX(ctx context.Context) int {
	count, err := coq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (coq *CattleOutQuery) Exist(ctx context.Context) (bool, error) {
	if err := coq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return coq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (coq *CattleOutQuery) ExistX(ctx context.Context) bool {
	exist, err := coq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CattleOutQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (coq *CattleOutQuery) Clone() *CattleOutQuery {
	if coq == nil {
		return nil
	}
	return &CattleOutQuery{
		config:     coq.config,
		limit:      coq.limit,
		offset:     coq.offset,
		order:      append([]OrderFunc{}, coq.order...),
		predicates: append([]predicate.CattleOut{}, coq.predicates...),
		// clone intermediate query.
		sql:    coq.sql.Clone(),
		path:   coq.path,
		unique: coq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		OutType int64 `json:"outType,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CattleOut.Query().
//		GroupBy(cattleout.FieldOutType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (coq *CattleOutQuery) GroupBy(field string, fields ...string) *CattleOutGroupBy {
	grbuild := &CattleOutGroupBy{config: coq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := coq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return coq.sqlQuery(ctx), nil
	}
	grbuild.label = cattleout.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		OutType int64 `json:"outType,omitempty"`
//	}
//
//	client.CattleOut.Query().
//		Select(cattleout.FieldOutType).
//		Scan(ctx, &v)
func (coq *CattleOutQuery) Select(fields ...string) *CattleOutSelect {
	coq.fields = append(coq.fields, fields...)
	selbuild := &CattleOutSelect{CattleOutQuery: coq}
	selbuild.label = cattleout.Label
	selbuild.flds, selbuild.scan = &coq.fields, selbuild.Scan
	return selbuild
}

func (coq *CattleOutQuery) prepareQuery(ctx context.Context) error {
	for _, f := range coq.fields {
		if !cattleout.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if coq.path != nil {
		prev, err := coq.path(ctx)
		if err != nil {
			return err
		}
		coq.sql = prev
	}
	return nil
}

func (coq *CattleOutQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CattleOut, error) {
	var (
		nodes = []*CattleOut{}
		_spec = coq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*CattleOut).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &CattleOut{config: coq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, coq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (coq *CattleOutQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := coq.querySpec()
	_spec.Node.Columns = coq.fields
	if len(coq.fields) > 0 {
		_spec.Unique = coq.unique != nil && *coq.unique
	}
	return sqlgraph.CountNodes(ctx, coq.driver, _spec)
}

func (coq *CattleOutQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := coq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (coq *CattleOutQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   cattleout.Table,
			Columns: cattleout.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: cattleout.FieldID,
			},
		},
		From:   coq.sql,
		Unique: true,
	}
	if unique := coq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := coq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, cattleout.FieldID)
		for i := range fields {
			if fields[i] != cattleout.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := coq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := coq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := coq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := coq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (coq *CattleOutQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(coq.driver.Dialect())
	t1 := builder.Table(cattleout.Table)
	columns := coq.fields
	if len(columns) == 0 {
		columns = cattleout.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if coq.sql != nil {
		selector = coq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if coq.unique != nil && *coq.unique {
		selector.Distinct()
	}
	for _, p := range coq.predicates {
		p(selector)
	}
	for _, p := range coq.order {
		p(selector)
	}
	if offset := coq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := coq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CattleOutGroupBy is the group-by builder for CattleOut entities.
type CattleOutGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cogb *CattleOutGroupBy) Aggregate(fns ...AggregateFunc) *CattleOutGroupBy {
	cogb.fns = append(cogb.fns, fns...)
	return cogb
}

// Scan applies the group-by query and scans the result into the given value.
func (cogb *CattleOutGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := cogb.path(ctx)
	if err != nil {
		return err
	}
	cogb.sql = query
	return cogb.sqlScan(ctx, v)
}

func (cogb *CattleOutGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range cogb.fields {
		if !cattleout.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := cogb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cogb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cogb *CattleOutGroupBy) sqlQuery() *sql.Selector {
	selector := cogb.sql.Select()
	aggregation := make([]string, 0, len(cogb.fns))
	for _, fn := range cogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(cogb.fields)+len(cogb.fns))
		for _, f := range cogb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(cogb.fields...)...)
}

// CattleOutSelect is the builder for selecting fields of CattleOut entities.
type CattleOutSelect struct {
	*CattleOutQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (cos *CattleOutSelect) Scan(ctx context.Context, v interface{}) error {
	if err := cos.prepareQuery(ctx); err != nil {
		return err
	}
	cos.sql = cos.CattleOutQuery.sqlQuery(ctx)
	return cos.sqlScan(ctx, v)
}

func (cos *CattleOutSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := cos.sql.Query()
	if err := cos.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/cattletype"
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
)

// CattleTypeCreate is the builder for creating a CattleType entity.
type CattleTypeCreate struct {
	config
	mutation *CattleTypeMutation
	hooks    []Hook
}

// SetName sets the name field.
func (ctc *CattleTypeCreate) SetName(s string) *CattleTypeCreate {
	ctc.mutation.SetName(s)
	return ctc
}

// Mutation returns the CattleTypeMutation object of the builder.
func (ctc *CattleTypeCreate) Mutation() *CattleTypeMutation {
	return ctc.mutation
}

// Save creates the CattleType in the database.
func (ctc *CattleTypeCreate) Save(ctx context.Context) (*CattleType, error) {
	var (
		err  error
		node *CattleType
	)
	if len(ctc.hooks) == 0 {
		if err = ctc.check(); err != nil {
			return nil, err
		}
		node, err = ctc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*CattleTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ctc.check(); err != nil {
				return nil, err
			}
			ctc.mutation = mutation
			node, err = ctc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(ctc.hooks) - 1; i >= 0; i-- {
			mut = ctc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ctc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (ctc *CattleTypeCreate) SaveX(ctx context.Context) *CattleType {
	v, err := ctc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (ctc *CattleTypeCreate) check() error {
	if _, ok := ctc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := ctc.mutation.Name(); ok {
		if err := cattletype.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (ctc *CattleTypeCreate) sqlSave(ctx context.Context) (*CattleType, error) {
	_node, _spec := ctc.createSpec()
	if err := sqlgraph.CreateNode(ctx, ctc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int64(id)
	return _node, nil
}

func (ctc *CattleTypeCreate) createSpec() (*CattleType, *sqlgraph.CreateSpec) {
	var (
		_node = &CattleType{config: ctc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: cattletype.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: cattletype.FieldID,
			},
		}
	)
	if value, ok := ctc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: cattletype.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// CattleTypeCreateBulk is the builder for creating a bulk of CattleType entities.
type CattleTypeCreateBulk struct {
	config
	builders []*CattleTypeCreate
}

// Save creates the CattleType entities in the database.
func (ctcb *CattleTypeCreateBulk) Save(ctx context.Context) ([]*CattleType, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ctcb.builders))
	nodes := make([]*CattleType, len(ctcb.builders))
	mutators := make([]Mutator, len(ctcb.builders))
	for i := range ctcb.builders {
		func(i int, root context.Context) {
			builder := ctcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*CattleTypeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ctcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ctcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int64(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ctcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (ctcb *CattleTypeCreateBulk) SaveX(ctx context.Context) []*CattleType {
	v, err := ctcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/concentrateformula"
	"cattleai/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ConcentrateFormulaQuery is the builder for querying ConcentrateFormula entities.
type ConcentrateFormulaQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ConcentrateFormula
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ConcentrateFormulaQuery builder.
func (cfq *ConcentrateFormulaQuery) Where(ps ...predicate.ConcentrateFormula) *ConcentrateFormulaQuery {
	cfq.predicates = append(cfq.predicates, ps...)
	return cfq
}

// Limit adds a limit step to the query.
func (cfq *ConcentrateFormulaQuery) Limit(limit int) *ConcentrateFormulaQuery {
	cfq.limit = &limit
	return cfq
}

// Offset adds an offset step to the query.
func (cfq *ConcentrateFormulaQuery) Offset(offset int) *ConcentrateFormulaQuery {
	cfq.offset = &offset
	return cfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cfq *ConcentrateFormulaQuery) Unique(unique bool) *ConcentrateFormulaQuery {
	cfq.unique = &unique
	return cfq
}

// Order adds an order step to the query.
func (cfq *ConcentrateFormulaQuery) Order(o ...OrderFunc) *ConcentrateFormulaQuery {
	cfq.order = append(cfq.order, o...)
	return cfq
}

// First returns the first ConcentrateFormula entity from the query.
// Returns a *NotFoundError when no ConcentrateFormula was found.
func (cfq *ConcentrateFormulaQuery) First(ctx context.Context) (*ConcentrateFormula, error) {
	nodes, err := cfq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{concentrateformula.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cfq *ConcentrateFormulaQuery) FirstX(ctx context.Context) *ConcentrateFormula {
	node, err := cfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ConcentrateFormula ID from the query.
// Returns a *NotFoundError when no ConcentrateFormula ID was found.
func (cfq *ConcentrateFormulaQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cfq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{concentrateformula.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cfq *ConcentrateFormulaQuery) FirstIDX(ctx context.Context) int {
	id, err := cfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ConcentrateFormula entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ConcentrateFormula entity is found.
// Returns a *NotFoundError when no ConcentrateFormula entities are found.
func (cfq *ConcentrateFormulaQuery) Only(ctx context.Context) (*ConcentrateFormula, error) {
	nodes, err := cfq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{concentrateformula.Label}
	default:
		return nil, &NotSingularError{concentrateformula.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cfq *ConcentrateFormulaQuery) OnlyX(ctx context.Context) *ConcentrateFormula {
	node, err := cfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ConcentrateFormula ID in the query.
// Returns a *NotSingularError when more than one ConcentrateFormula ID is found.
// Returns a *NotFoundError when no entities are found.
func (cfq *ConcentrateFormulaQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cfq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{concentrateformula.Label}
	default:
		err = &NotSingularError{concentrateformula.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cfq *ConcentrateFormulaQuery) OnlyIDX(ctx context.Context) int {
	id, err := cfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ConcentrateFormulas.
func (cfq *ConcentrateFormulaQuery) All(ctx context.Context) ([]*ConcentrateFormula, error) {
	if err := cfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return cfq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (cfq *ConcentrateFormulaQuery) AllX(ctx context.Context) []*ConcentrateFormula {
	nodes, err := cfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ConcentrateFormula IDs.
func (cfq *ConcentrateFormulaQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := cfq.Select(concentrateformula.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cfq *ConcentrateFormulaQuery) IDsX(ctx context.Context) []int {
	ids, err := cfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cfq *ConcentrateFormulaQuery) Count(ctx context.Context) (int, error) {
	if err := cfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return cfq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (cfq *ConcentrateFormulaQuery) CountX(ctx context.Context) int {
	count, err := cfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cfq *ConcentrateFormulaQuery) Exist(ctx context.Context) (bool, error) {
	if err := cfq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return cfq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (cfq *ConcentrateFormulaQuery) ExistX(ctx context.Context) bool {
	exist, err := cfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ConcentrateFormulaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cfq *ConcentrateFormulaQuery) Clone() *ConcentrateFormulaQuery {
	if cfq == nil {
		return nil
	}
	return &ConcentrateFormulaQuery{
		config:     cfq.config,
		limit:      cfq.limit,
		offset:     cfq.offset,
		order:      append([]OrderFunc{}, cfq.order...),
		predicates: append([]predicate.ConcentrateFormula{}, cfq.predicates...),
		// clone intermediate query.
		sql:    cfq.sql.Clone(),
		path:   cfq.path,
		unique: cfq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ConcentrateFormula.Query().
//		GroupBy(concentrateformula.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cfq *ConcentrateFormulaQuery) GroupBy(field string, fields ...string) *ConcentrateFormulaGroupBy {
	grbuild := &ConcentrateFormulaGroupBy{config: cfq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := cfq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return cfq.sqlQuery(ctx), nil
	}
	grbuild.label = concentrateformula.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.ConcentrateFormula.Query().
//		Select(concentrateformula.FieldName).
//		Scan(ctx, &v)
func (cfq *ConcentrateFormulaQuery) Select(fields ...string) *ConcentrateFormulaSelect {
	cfq.fields = append(cfq.fields, fields...)
	selbuild := &ConcentrateFormulaSelect{ConcentrateFormulaQuery: cfq}
	selbuild.label = concentrateformula.Label
	selbuild.flds, selbuild.scan = &cfq.fields, selbuild.Scan
	return selbuild
}

func (cfq *ConcentrateFormulaQuery) prepareQuery(ctx context.Context) error {
	for _, f := range cfq.fields {
		if !concentrateformula.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cfq.path != nil {
		prev, err := cfq.path(ctx)
		if err != nil {
			return err
		}
		cfq.sql = prev
	}
	return nil
}

func (cfq *ConcentrateFormulaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ConcentrateFormula, error) {
	var (
		nodes = []*ConcentrateFormula{}
		_spec = cfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*ConcentrateFormula).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &ConcentrateFormula{config: cfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cfq *ConcentrateFormulaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cfq.querySpec()
	_spec.Node.Columns = cfq.fields
	if len(cfq.fields) > 0 {
		_spec.Unique = cfq.unique != nil && *cfq.unique
	}
	return sqlgraph.CountNodes(ctx, cfq.driver, _spec)
}

func (cfq *ConcentrateFormulaQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := cfq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (cfq *ConcentrateFormulaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   concentrateformula.Table,
			Columns: concentrateformula.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: concentrateformula.FieldID,
			},
		},
		From:   cfq.sql,
		Unique: true,
	}
	if unique := cfq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := cfq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, concentrateformula.FieldID)
		for i := range fields {
			if fields[i] != concentrateformula.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cfq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cfq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cfq *ConcentrateFormulaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cfq.driver.Dialect())
	t1 := builder.Table(concentrateformula.Table)
	columns := cfq.fields
	if len(columns) == 0 {
		columns = concentrateformula.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cfq.sql != nil {
		selector = cfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cfq.unique != nil && *cfq.unique {
		selector.Distinct()
	}
	for _, p := range cfq.predicates {
		p(selector)
	}
	for _, p := range cfq.order {
		p(selector)
	}
	if offset := cfq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cfq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ConcentrateFormulaGroupBy is the group-by builder for ConcentrateFormula entities.
type ConcentrateFormulaGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cfgb *ConcentrateFormulaGroupBy) Aggregate(fns ...AggregateFunc) *ConcentrateFormulaGroupBy {
	cfgb.fns = append(cfgb.fns, fns...)
	return cfgb
}

// Scan applies the group-by query and scans the result into the given value.
func (cfgb *ConcentrateFormulaGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := cfgb.path(ctx)
	if err != nil {
		return err
	}
	cfgb.sql = query
	return cfgb.sqlScan(ctx, v)
}

func (cfgb *ConcentrateFormulaGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range cfgb.fields {
		if !concentrateformula.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := cfgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cfgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cfgb *ConcentrateFormulaGroupBy) sqlQuery() *sql.Selector {
	selector := cfgb.sql.Select()
	aggregation := make([]string, 0, len(cfgb.fns))
	for _, fn := range cfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(cfgb.fields)+len(cfgb.fns))
		for _, f := range cfgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(cfgb.fields...)...)
}

// ConcentrateFormulaSelect is the builder for selecting fields of ConcentrateFormula entities.
type ConcentrateFormulaSelect struct {
	*ConcentrateFormulaQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (cfs *ConcentrateFormulaSelect) Scan(ctx context.Context, v interface{}) error {
	if err := cfs.prepareQuery(ctx); err != nil {
		return err
	}
	cfs.sql = cfs.ConcentrateFormulaQuery.sqlQuery(ctx)
	return cfs.sqlScan(ctx, v)
}

func (cfs *ConcentrateFormulaSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := cfs.sql.Query()
	if err := cfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

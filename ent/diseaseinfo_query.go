// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/diseaseinfo"
	"cattleai/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// DiseaseInfoQuery is the builder for querying DiseaseInfo entities.
type DiseaseInfoQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.DiseaseInfo
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DiseaseInfoQuery builder.
func (diq *DiseaseInfoQuery) Where(ps ...predicate.DiseaseInfo) *DiseaseInfoQuery {
	diq.predicates = append(diq.predicates, ps...)
	return diq
}

// Limit adds a limit step to the query.
func (diq *DiseaseInfoQuery) Limit(limit int) *DiseaseInfoQuery {
	diq.limit = &limit
	return diq
}

// Offset adds an offset step to the query.
func (diq *DiseaseInfoQuery) Offset(offset int) *DiseaseInfoQuery {
	diq.offset = &offset
	return diq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (diq *DiseaseInfoQuery) Unique(unique bool) *DiseaseInfoQuery {
	diq.unique = &unique
	return diq
}

// Order adds an order step to the query.
func (diq *DiseaseInfoQuery) Order(o ...OrderFunc) *DiseaseInfoQuery {
	diq.order = append(diq.order, o...)
	return diq
}

// First returns the first DiseaseInfo entity from the query.
// Returns a *NotFoundError when no DiseaseInfo was found.
func (diq *DiseaseInfoQuery) First(ctx context.Context) (*DiseaseInfo, error) {
	nodes, err := diq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{diseaseinfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (diq *DiseaseInfoQuery) FirstX(ctx context.Context) *DiseaseInfo {
	node, err := diq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DiseaseInfo ID from the query.
// Returns a *NotFoundError when no DiseaseInfo ID was found.
func (diq *DiseaseInfoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = diq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{diseaseinfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (diq *DiseaseInfoQuery) FirstIDX(ctx context.Context) int {
	id, err := diq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DiseaseInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DiseaseInfo entity is found.
// Returns a *NotFoundError when no DiseaseInfo entities are found.
func (diq *DiseaseInfoQuery) Only(ctx context.Context) (*DiseaseInfo, error) {
	nodes, err := diq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{diseaseinfo.Label}
	default:
		return nil, &NotSingularError{diseaseinfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (diq *DiseaseInfoQuery) OnlyX(ctx context.Context) *DiseaseInfo {
	node, err := diq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DiseaseInfo ID in the query.
// Returns a *NotSingularError when more than one DiseaseInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (diq *DiseaseInfoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = diq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{diseaseinfo.Label}
	default:
		err = &NotSingularError{diseaseinfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (diq *DiseaseInfoQuery) OnlyIDX(ctx context.Context) int {
	id, err := diq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DiseaseInfos.
func (diq *DiseaseInfoQuery) All(ctx context.Context) ([]*DiseaseInfo, error) {
	if err := diq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return diq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (diq *DiseaseInfoQuery) AllX(ctx context.Context) []*DiseaseInfo {
	nodes, err := diq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DiseaseInfo IDs.
func (diq *DiseaseInfoQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := diq.Select(diseaseinfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (diq *DiseaseInfoQuery) IDsX(ctx context.Context) []int {
	ids, err := diq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (diq *DiseaseInfoQuery) Count(ctx context.Context) (int, error) {
	if err := diq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return diq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (diq *DiseaseInfoQuery) CountX(ctx context.Context) int {
	count, err := diq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (diq *DiseaseInfoQuery) Exist(ctx context.Context) (bool, error) {
	if err := diq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return diq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (diq *DiseaseInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := diq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DiseaseInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (diq *DiseaseInfoQuery) Clone() *DiseaseInfoQuery {
	if diq == nil {
		return nil
	}
	return &DiseaseInfoQuery{
		config:     diq.config,
		limit:      diq.limit,
		offset:     diq.offset,
		order:      append([]OrderFunc{}, diq.order...),
		predicates: append([]predicate.DiseaseInfo{}, diq.predicates...),
		// clone intermediate query.
		sql:    diq.sql.Clone(),
		path:   diq.path,
		unique: diq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DiseaseInfo.Query().
//		GroupBy(diseaseinfo.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (diq *DiseaseInfoQuery) GroupBy(field string, fields ...string) *DiseaseInfoGroupBy {
	grbuild := &DiseaseInfoGroupBy{config: diq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := diq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return diq.sqlQuery(ctx), nil
	}
	grbuild.label = diseaseinfo.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.DiseaseInfo.Query().
//		Select(diseaseinfo.FieldName).
//		Scan(ctx, &v)
func (diq *DiseaseInfoQuery) Select(fields ...string) *DiseaseInfoSelect {
	diq.fields = append(diq.fields, fields...)
	selbuild := &DiseaseInfoSelect{DiseaseInfoQuery: diq}
	selbuild.label = diseaseinfo.Label
	selbuild.flds, selbuild.scan = &diq.fields, selbuild.Scan
	return selbuild
}

func (diq *DiseaseInfoQuery) prepareQuery(ctx context.Context) error {
	for _, f := range diq.fields {
		if !diseaseinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if diq.path != nil {
		prev, err := diq.path(ctx)
		if err != nil {
			return err
		}
		diq.sql = prev
	}
	return nil
}

func (diq *DiseaseInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DiseaseInfo, error) {
	var (
		nodes = []*DiseaseInfo{}
		_spec = diq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*DiseaseInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &DiseaseInfo{config: diq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, diq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (diq *DiseaseInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := diq.querySpec()
	_spec.Node.Columns = diq.fields
	if len(diq.fields) > 0 {
		_spec.Unique = diq.unique != nil && *diq.unique
	}
	return sqlgraph.CountNodes(ctx, diq.driver, _spec)
}

func (diq *DiseaseInfoQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := diq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (diq *DiseaseInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   diseaseinfo.Table,
			Columns: diseaseinfo.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: diseaseinfo.FieldID,
			},
		},
		From:   diq.sql,
		Unique: true,
	}
	if unique := diq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := diq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, diseaseinfo.FieldID)
		for i := range fields {
			if fields[i] != diseaseinfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := diq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := diq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := diq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := diq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (diq *DiseaseInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(diq.driver.Dialect())
	t1 := builder.Table(diseaseinfo.Table)
	columns := diq.fields
	if len(columns) == 0 {
		columns = diseaseinfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if diq.sql != nil {
		selector = diq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if diq.unique != nil && *diq.unique {
		selector.Distinct()
	}
	for _, p := range diq.predicates {
		p(selector)
	}
	for _, p := range diq.order {
		p(selector)
	}
	if offset := diq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := diq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DiseaseInfoGroupBy is the group-by builder for DiseaseInfo entities.
type DiseaseInfoGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (digb *DiseaseInfoGroupBy) Aggregate(fns ...AggregateFunc) *DiseaseInfoGroupBy {
	digb.fns = append(digb.fns, fns...)
	return digb
}

// Scan applies the group-by query and scans the result into the given value.
func (digb *DiseaseInfoGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := digb.path(ctx)
	if err != nil {
		return err
	}
	digb.sql = query
	return digb.sqlScan(ctx, v)
}

func (digb *DiseaseInfoGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range digb.fields {
		if !diseaseinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := digb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := digb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (digb *DiseaseInfoGroupBy) sqlQuery() *sql.Selector {
	selector := digb.sql.Select()
	aggregation := make([]string, 0, len(digb.fns))
	for _, fn := range digb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(digb.fields)+len(digb.fns))
		for _, f := range digb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(digb.fields...)...)
}

// DiseaseInfoSelect is the builder for selecting fields of DiseaseInfo entities.
type DiseaseInfoSelect struct {
	*DiseaseInfoQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (dis *DiseaseInfoSelect) Scan(ctx context.Context, v interface{}) error {
	if err := dis.prepareQuery(ctx); err != nil {
		return err
	}
	dis.sql = dis.DiseaseInfoQuery.sqlQuery(ctx)
	return dis.sqlScan(ctx, v)
}

func (dis *DiseaseInfoSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := dis.sql.Query()
	if err := dis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

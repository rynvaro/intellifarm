// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/dispence"
	"context"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
)

// DispenceCreate is the builder for creating a Dispence entity.
type DispenceCreate struct {
	config
	mutation *DispenceMutation
	hooks    []Hook
}

// Mutation returns the DispenceMutation object of the builder.
func (dc *DispenceCreate) Mutation() *DispenceMutation {
	return dc.mutation
}

// Save creates the Dispence in the database.
func (dc *DispenceCreate) Save(ctx context.Context) (*Dispence, error) {
	var (
		err  error
		node *Dispence
	)
	if len(dc.hooks) == 0 {
		if err = dc.check(); err != nil {
			return nil, err
		}
		node, err = dc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*DispenceMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = dc.check(); err != nil {
				return nil, err
			}
			dc.mutation = mutation
			node, err = dc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(dc.hooks) - 1; i >= 0; i-- {
			mut = dc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, dc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (dc *DispenceCreate) SaveX(ctx context.Context) *Dispence {
	v, err := dc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (dc *DispenceCreate) check() error {
	return nil
}

func (dc *DispenceCreate) sqlSave(ctx context.Context) (*Dispence, error) {
	_node, _spec := dc.createSpec()
	if err := sqlgraph.CreateNode(ctx, dc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int64(id)
	return _node, nil
}

func (dc *DispenceCreate) createSpec() (*Dispence, *sqlgraph.CreateSpec) {
	var (
		_node = &Dispence{config: dc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: dispence.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: dispence.FieldID,
			},
		}
	)
	return _node, _spec
}

// DispenceCreateBulk is the builder for creating a bulk of Dispence entities.
type DispenceCreateBulk struct {
	config
	builders []*DispenceCreate
}

// Save creates the Dispence entities in the database.
func (dcb *DispenceCreateBulk) Save(ctx context.Context) ([]*Dispence, error) {
	specs := make([]*sqlgraph.CreateSpec, len(dcb.builders))
	nodes := make([]*Dispence, len(dcb.builders))
	mutators := make([]Mutator, len(dcb.builders))
	for i := range dcb.builders {
		func(i int, root context.Context) {
			builder := dcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*DispenceMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, dcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, dcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int64(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, dcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (dcb *DispenceCreateBulk) SaveX(ctx context.Context) []*Dispence {
	v, err := dcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

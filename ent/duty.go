// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/duty"
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// Duty is the model entity for the Duty schema.
type Duty struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 职责名称
	Name string `json:"name,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Duty) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case duty.FieldID:
			values[i] = new(sql.NullInt64)
		case duty.FieldName:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type Duty", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Duty fields.
func (d *Duty) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case duty.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			d.ID = int(value.Int64)
		case duty.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				d.Name = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this Duty.
// Note that you need to call Duty.Unwrap() before calling this method if this Duty
// was returned from a transaction, and the transaction was committed or rolled back.
func (d *Duty) Update() *DutyUpdateOne {
	return (&DutyClient{config: d.config}).UpdateOne(d)
}

// Unwrap unwraps the Duty entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (d *Duty) Unwrap() *Duty {
	_tx, ok := d.config.driver.(*txDriver)
	if !ok {
		panic("ent: Duty is not a transactional entity")
	}
	d.config.driver = _tx.drv
	return d
}

// String implements the fmt.Stringer.
func (d *Duty) String() string {
	var builder strings.Builder
	builder.WriteString("Duty(")
	builder.WriteString(fmt.Sprintf("id=%v, ", d.ID))
	builder.WriteString("name=")
	builder.WriteString(d.Name)
	builder.WriteByte(')')
	return builder.String()
}

// Duties is a parsable slice of Duty.
type Duties []*Duty

func (d Duties) config(cfg config) {
	for _i := range d {
		d[_i].config = cfg
	}
}

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/feedgroup"
	"fmt"
	"strings"

	"github.com/facebook/ent/dialect/sql"
)

// FeedGroup is the model entity for the FeedGroup schema.
type FeedGroup struct {
	config
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*FeedGroup) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{}, // id
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the FeedGroup fields.
func (fg *FeedGroup) assignValues(values ...interface{}) error {
	if m, n := len(values), len(feedgroup.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	fg.ID = int64(value.Int64)
	values = values[1:]
	return nil
}

// Update returns a builder for updating this FeedGroup.
// Note that, you need to call FeedGroup.Unwrap() before calling this method, if this FeedGroup
// was returned from a transaction, and the transaction was committed or rolled back.
func (fg *FeedGroup) Update() *FeedGroupUpdateOne {
	return (&FeedGroupClient{config: fg.config}).UpdateOne(fg)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (fg *FeedGroup) Unwrap() *FeedGroup {
	tx, ok := fg.config.driver.(*txDriver)
	if !ok {
		panic("ent: FeedGroup is not a transactional entity")
	}
	fg.config.driver = tx.drv
	return fg
}

// String implements the fmt.Stringer.
func (fg *FeedGroup) String() string {
	var builder strings.Builder
	builder.WriteString("FeedGroup(")
	builder.WriteString(fmt.Sprintf("id=%v", fg.ID))
	builder.WriteByte(')')
	return builder.String()
}

// FeedGroups is a parsable slice of FeedGroup.
type FeedGroups []*FeedGroup

func (fg FeedGroups) config(cfg config) {
	for _i := range fg {
		fg[_i].config = cfg
	}
}

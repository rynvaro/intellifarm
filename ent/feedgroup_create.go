// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/feedgroup"
	"context"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FeedGroupCreate is the builder for creating a FeedGroup entity.
type FeedGroupCreate struct {
	config
	mutation *FeedGroupMutation
	hooks    []Hook
}

// Mutation returns the FeedGroupMutation object of the builder.
func (fgc *FeedGroupCreate) Mutation() *FeedGroupMutation {
	return fgc.mutation
}

// Save creates the FeedGroup in the database.
func (fgc *FeedGroupCreate) Save(ctx context.Context) (*FeedGroup, error) {
	var (
		err  error
		node *FeedGroup
	)
	if len(fgc.hooks) == 0 {
		if err = fgc.check(); err != nil {
			return nil, err
		}
		node, err = fgc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*FeedGroupMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = fgc.check(); err != nil {
				return nil, err
			}
			fgc.mutation = mutation
			if node, err = fgc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(fgc.hooks) - 1; i >= 0; i-- {
			if fgc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = fgc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, fgc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*FeedGroup)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from FeedGroupMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (fgc *FeedGroupCreate) SaveX(ctx context.Context) *FeedGroup {
	v, err := fgc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (fgc *FeedGroupCreate) Exec(ctx context.Context) error {
	_, err := fgc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (fgc *FeedGroupCreate) ExecX(ctx context.Context) {
	if err := fgc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (fgc *FeedGroupCreate) check() error {
	return nil
}

func (fgc *FeedGroupCreate) sqlSave(ctx context.Context) (*FeedGroup, error) {
	_node, _spec := fgc.createSpec()
	if err := sqlgraph.CreateNode(ctx, fgc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (fgc *FeedGroupCreate) createSpec() (*FeedGroup, *sqlgraph.CreateSpec) {
	var (
		_node = &FeedGroup{config: fgc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: feedgroup.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: feedgroup.FieldID,
			},
		}
	)
	return _node, _spec
}

// FeedGroupCreateBulk is the builder for creating many FeedGroup entities in bulk.
type FeedGroupCreateBulk struct {
	config
	builders []*FeedGroupCreate
}

// Save creates the FeedGroup entities in the database.
func (fgcb *FeedGroupCreateBulk) Save(ctx context.Context) ([]*FeedGroup, error) {
	specs := make([]*sqlgraph.CreateSpec, len(fgcb.builders))
	nodes := make([]*FeedGroup, len(fgcb.builders))
	mutators := make([]Mutator, len(fgcb.builders))
	for i := range fgcb.builders {
		func(i int, root context.Context) {
			builder := fgcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*FeedGroupMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, fgcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, fgcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, fgcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (fgcb *FeedGroupCreateBulk) SaveX(ctx context.Context) []*FeedGroup {
	v, err := fgcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (fgcb *FeedGroupCreateBulk) Exec(ctx context.Context) error {
	_, err := fgcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (fgcb *FeedGroupCreateBulk) ExecX(ctx context.Context) {
	if err := fgcb.Exec(ctx); err != nil {
		panic(err)
	}
}

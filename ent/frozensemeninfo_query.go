// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/frozensemeninfo"
	"cattleai/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FrozenSemenInfoQuery is the builder for querying FrozenSemenInfo entities.
type FrozenSemenInfoQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.FrozenSemenInfo
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FrozenSemenInfoQuery builder.
func (fsiq *FrozenSemenInfoQuery) Where(ps ...predicate.FrozenSemenInfo) *FrozenSemenInfoQuery {
	fsiq.predicates = append(fsiq.predicates, ps...)
	return fsiq
}

// Limit adds a limit step to the query.
func (fsiq *FrozenSemenInfoQuery) Limit(limit int) *FrozenSemenInfoQuery {
	fsiq.limit = &limit
	return fsiq
}

// Offset adds an offset step to the query.
func (fsiq *FrozenSemenInfoQuery) Offset(offset int) *FrozenSemenInfoQuery {
	fsiq.offset = &offset
	return fsiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fsiq *FrozenSemenInfoQuery) Unique(unique bool) *FrozenSemenInfoQuery {
	fsiq.unique = &unique
	return fsiq
}

// Order adds an order step to the query.
func (fsiq *FrozenSemenInfoQuery) Order(o ...OrderFunc) *FrozenSemenInfoQuery {
	fsiq.order = append(fsiq.order, o...)
	return fsiq
}

// First returns the first FrozenSemenInfo entity from the query.
// Returns a *NotFoundError when no FrozenSemenInfo was found.
func (fsiq *FrozenSemenInfoQuery) First(ctx context.Context) (*FrozenSemenInfo, error) {
	nodes, err := fsiq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{frozensemeninfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fsiq *FrozenSemenInfoQuery) FirstX(ctx context.Context) *FrozenSemenInfo {
	node, err := fsiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FrozenSemenInfo ID from the query.
// Returns a *NotFoundError when no FrozenSemenInfo ID was found.
func (fsiq *FrozenSemenInfoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fsiq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{frozensemeninfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fsiq *FrozenSemenInfoQuery) FirstIDX(ctx context.Context) int {
	id, err := fsiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FrozenSemenInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FrozenSemenInfo entity is found.
// Returns a *NotFoundError when no FrozenSemenInfo entities are found.
func (fsiq *FrozenSemenInfoQuery) Only(ctx context.Context) (*FrozenSemenInfo, error) {
	nodes, err := fsiq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{frozensemeninfo.Label}
	default:
		return nil, &NotSingularError{frozensemeninfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fsiq *FrozenSemenInfoQuery) OnlyX(ctx context.Context) *FrozenSemenInfo {
	node, err := fsiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FrozenSemenInfo ID in the query.
// Returns a *NotSingularError when more than one FrozenSemenInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (fsiq *FrozenSemenInfoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fsiq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{frozensemeninfo.Label}
	default:
		err = &NotSingularError{frozensemeninfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fsiq *FrozenSemenInfoQuery) OnlyIDX(ctx context.Context) int {
	id, err := fsiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FrozenSemenInfos.
func (fsiq *FrozenSemenInfoQuery) All(ctx context.Context) ([]*FrozenSemenInfo, error) {
	if err := fsiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return fsiq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (fsiq *FrozenSemenInfoQuery) AllX(ctx context.Context) []*FrozenSemenInfo {
	nodes, err := fsiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FrozenSemenInfo IDs.
func (fsiq *FrozenSemenInfoQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := fsiq.Select(frozensemeninfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fsiq *FrozenSemenInfoQuery) IDsX(ctx context.Context) []int {
	ids, err := fsiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fsiq *FrozenSemenInfoQuery) Count(ctx context.Context) (int, error) {
	if err := fsiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return fsiq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (fsiq *FrozenSemenInfoQuery) CountX(ctx context.Context) int {
	count, err := fsiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fsiq *FrozenSemenInfoQuery) Exist(ctx context.Context) (bool, error) {
	if err := fsiq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return fsiq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (fsiq *FrozenSemenInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := fsiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FrozenSemenInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fsiq *FrozenSemenInfoQuery) Clone() *FrozenSemenInfoQuery {
	if fsiq == nil {
		return nil
	}
	return &FrozenSemenInfoQuery{
		config:     fsiq.config,
		limit:      fsiq.limit,
		offset:     fsiq.offset,
		order:      append([]OrderFunc{}, fsiq.order...),
		predicates: append([]predicate.FrozenSemenInfo{}, fsiq.predicates...),
		// clone intermediate query.
		sql:    fsiq.sql.Clone(),
		path:   fsiq.path,
		unique: fsiq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FrozenSemenInfo.Query().
//		GroupBy(frozensemeninfo.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fsiq *FrozenSemenInfoQuery) GroupBy(field string, fields ...string) *FrozenSemenInfoGroupBy {
	grbuild := &FrozenSemenInfoGroupBy{config: fsiq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := fsiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return fsiq.sqlQuery(ctx), nil
	}
	grbuild.label = frozensemeninfo.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.FrozenSemenInfo.Query().
//		Select(frozensemeninfo.FieldName).
//		Scan(ctx, &v)
func (fsiq *FrozenSemenInfoQuery) Select(fields ...string) *FrozenSemenInfoSelect {
	fsiq.fields = append(fsiq.fields, fields...)
	selbuild := &FrozenSemenInfoSelect{FrozenSemenInfoQuery: fsiq}
	selbuild.label = frozensemeninfo.Label
	selbuild.flds, selbuild.scan = &fsiq.fields, selbuild.Scan
	return selbuild
}

func (fsiq *FrozenSemenInfoQuery) prepareQuery(ctx context.Context) error {
	for _, f := range fsiq.fields {
		if !frozensemeninfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fsiq.path != nil {
		prev, err := fsiq.path(ctx)
		if err != nil {
			return err
		}
		fsiq.sql = prev
	}
	return nil
}

func (fsiq *FrozenSemenInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FrozenSemenInfo, error) {
	var (
		nodes = []*FrozenSemenInfo{}
		_spec = fsiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*FrozenSemenInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &FrozenSemenInfo{config: fsiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fsiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (fsiq *FrozenSemenInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fsiq.querySpec()
	_spec.Node.Columns = fsiq.fields
	if len(fsiq.fields) > 0 {
		_spec.Unique = fsiq.unique != nil && *fsiq.unique
	}
	return sqlgraph.CountNodes(ctx, fsiq.driver, _spec)
}

func (fsiq *FrozenSemenInfoQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := fsiq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (fsiq *FrozenSemenInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   frozensemeninfo.Table,
			Columns: frozensemeninfo.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: frozensemeninfo.FieldID,
			},
		},
		From:   fsiq.sql,
		Unique: true,
	}
	if unique := fsiq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := fsiq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, frozensemeninfo.FieldID)
		for i := range fields {
			if fields[i] != frozensemeninfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fsiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fsiq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fsiq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fsiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fsiq *FrozenSemenInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fsiq.driver.Dialect())
	t1 := builder.Table(frozensemeninfo.Table)
	columns := fsiq.fields
	if len(columns) == 0 {
		columns = frozensemeninfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fsiq.sql != nil {
		selector = fsiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fsiq.unique != nil && *fsiq.unique {
		selector.Distinct()
	}
	for _, p := range fsiq.predicates {
		p(selector)
	}
	for _, p := range fsiq.order {
		p(selector)
	}
	if offset := fsiq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fsiq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FrozenSemenInfoGroupBy is the group-by builder for FrozenSemenInfo entities.
type FrozenSemenInfoGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fsigb *FrozenSemenInfoGroupBy) Aggregate(fns ...AggregateFunc) *FrozenSemenInfoGroupBy {
	fsigb.fns = append(fsigb.fns, fns...)
	return fsigb
}

// Scan applies the group-by query and scans the result into the given value.
func (fsigb *FrozenSemenInfoGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := fsigb.path(ctx)
	if err != nil {
		return err
	}
	fsigb.sql = query
	return fsigb.sqlScan(ctx, v)
}

func (fsigb *FrozenSemenInfoGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range fsigb.fields {
		if !frozensemeninfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := fsigb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fsigb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (fsigb *FrozenSemenInfoGroupBy) sqlQuery() *sql.Selector {
	selector := fsigb.sql.Select()
	aggregation := make([]string, 0, len(fsigb.fns))
	for _, fn := range fsigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(fsigb.fields)+len(fsigb.fns))
		for _, f := range fsigb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(fsigb.fields...)...)
}

// FrozenSemenInfoSelect is the builder for selecting fields of FrozenSemenInfo entities.
type FrozenSemenInfoSelect struct {
	*FrozenSemenInfoQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (fsis *FrozenSemenInfoSelect) Scan(ctx context.Context, v interface{}) error {
	if err := fsis.prepareQuery(ctx); err != nil {
		return err
	}
	fsis.sql = fsis.FrozenSemenInfoQuery.sqlQuery(ctx)
	return fsis.sqlScan(ctx, v)
}

func (fsis *FrozenSemenInfoSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := fsis.sql.Query()
	if err := fsis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

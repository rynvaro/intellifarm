// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/hairstate"
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// HairStateCreate is the builder for creating a HairState entity.
type HairStateCreate struct {
	config
	mutation *HairStateMutation
	hooks    []Hook
}

// SetName sets the "name" field.
func (hsc *HairStateCreate) SetName(s string) *HairStateCreate {
	hsc.mutation.SetName(s)
	return hsc
}

// Mutation returns the HairStateMutation object of the builder.
func (hsc *HairStateCreate) Mutation() *HairStateMutation {
	return hsc.mutation
}

// Save creates the HairState in the database.
func (hsc *HairStateCreate) Save(ctx context.Context) (*HairState, error) {
	var (
		err  error
		node *HairState
	)
	if len(hsc.hooks) == 0 {
		if err = hsc.check(); err != nil {
			return nil, err
		}
		node, err = hsc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*HairStateMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = hsc.check(); err != nil {
				return nil, err
			}
			hsc.mutation = mutation
			if node, err = hsc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(hsc.hooks) - 1; i >= 0; i-- {
			if hsc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = hsc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, hsc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*HairState)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from HairStateMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (hsc *HairStateCreate) SaveX(ctx context.Context) *HairState {
	v, err := hsc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (hsc *HairStateCreate) Exec(ctx context.Context) error {
	_, err := hsc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (hsc *HairStateCreate) ExecX(ctx context.Context) {
	if err := hsc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (hsc *HairStateCreate) check() error {
	if _, ok := hsc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "HairState.name"`)}
	}
	if v, ok := hsc.mutation.Name(); ok {
		if err := hairstate.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf(`ent: validator failed for field "HairState.name": %w`, err)}
		}
	}
	return nil
}

func (hsc *HairStateCreate) sqlSave(ctx context.Context) (*HairState, error) {
	_node, _spec := hsc.createSpec()
	if err := sqlgraph.CreateNode(ctx, hsc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (hsc *HairStateCreate) createSpec() (*HairState, *sqlgraph.CreateSpec) {
	var (
		_node = &HairState{config: hsc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: hairstate.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: hairstate.FieldID,
			},
		}
	)
	if value, ok := hsc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: hairstate.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// HairStateCreateBulk is the builder for creating many HairState entities in bulk.
type HairStateCreateBulk struct {
	config
	builders []*HairStateCreate
}

// Save creates the HairState entities in the database.
func (hscb *HairStateCreateBulk) Save(ctx context.Context) ([]*HairState, error) {
	specs := make([]*sqlgraph.CreateSpec, len(hscb.builders))
	nodes := make([]*HairState, len(hscb.builders))
	mutators := make([]Mutator, len(hscb.builders))
	for i := range hscb.builders {
		func(i int, root context.Context) {
			builder := hscb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*HairStateMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, hscb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, hscb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, hscb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (hscb *HairStateCreateBulk) SaveX(ctx context.Context) []*HairState {
	v, err := hscb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (hscb *HairStateCreateBulk) Exec(ctx context.Context) error {
	_, err := hscb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (hscb *HairStateCreateBulk) ExecX(ctx context.Context) {
	if err := hscb.Exec(ctx); err != nil {
		panic(err)
	}
}

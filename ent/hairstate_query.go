// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/hairstate"
	"cattleai/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// HairStateQuery is the builder for querying HairState entities.
type HairStateQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.HairState
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HairStateQuery builder.
func (hsq *HairStateQuery) Where(ps ...predicate.HairState) *HairStateQuery {
	hsq.predicates = append(hsq.predicates, ps...)
	return hsq
}

// Limit adds a limit step to the query.
func (hsq *HairStateQuery) Limit(limit int) *HairStateQuery {
	hsq.limit = &limit
	return hsq
}

// Offset adds an offset step to the query.
func (hsq *HairStateQuery) Offset(offset int) *HairStateQuery {
	hsq.offset = &offset
	return hsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hsq *HairStateQuery) Unique(unique bool) *HairStateQuery {
	hsq.unique = &unique
	return hsq
}

// Order adds an order step to the query.
func (hsq *HairStateQuery) Order(o ...OrderFunc) *HairStateQuery {
	hsq.order = append(hsq.order, o...)
	return hsq
}

// First returns the first HairState entity from the query.
// Returns a *NotFoundError when no HairState was found.
func (hsq *HairStateQuery) First(ctx context.Context) (*HairState, error) {
	nodes, err := hsq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{hairstate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hsq *HairStateQuery) FirstX(ctx context.Context) *HairState {
	node, err := hsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HairState ID from the query.
// Returns a *NotFoundError when no HairState ID was found.
func (hsq *HairStateQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hsq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{hairstate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hsq *HairStateQuery) FirstIDX(ctx context.Context) int {
	id, err := hsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HairState entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HairState entity is found.
// Returns a *NotFoundError when no HairState entities are found.
func (hsq *HairStateQuery) Only(ctx context.Context) (*HairState, error) {
	nodes, err := hsq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{hairstate.Label}
	default:
		return nil, &NotSingularError{hairstate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hsq *HairStateQuery) OnlyX(ctx context.Context) *HairState {
	node, err := hsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HairState ID in the query.
// Returns a *NotSingularError when more than one HairState ID is found.
// Returns a *NotFoundError when no entities are found.
func (hsq *HairStateQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hsq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{hairstate.Label}
	default:
		err = &NotSingularError{hairstate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hsq *HairStateQuery) OnlyIDX(ctx context.Context) int {
	id, err := hsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HairStates.
func (hsq *HairStateQuery) All(ctx context.Context) ([]*HairState, error) {
	if err := hsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return hsq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (hsq *HairStateQuery) AllX(ctx context.Context) []*HairState {
	nodes, err := hsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HairState IDs.
func (hsq *HairStateQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := hsq.Select(hairstate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hsq *HairStateQuery) IDsX(ctx context.Context) []int {
	ids, err := hsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hsq *HairStateQuery) Count(ctx context.Context) (int, error) {
	if err := hsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return hsq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (hsq *HairStateQuery) CountX(ctx context.Context) int {
	count, err := hsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hsq *HairStateQuery) Exist(ctx context.Context) (bool, error) {
	if err := hsq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return hsq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (hsq *HairStateQuery) ExistX(ctx context.Context) bool {
	exist, err := hsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HairStateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hsq *HairStateQuery) Clone() *HairStateQuery {
	if hsq == nil {
		return nil
	}
	return &HairStateQuery{
		config:     hsq.config,
		limit:      hsq.limit,
		offset:     hsq.offset,
		order:      append([]OrderFunc{}, hsq.order...),
		predicates: append([]predicate.HairState{}, hsq.predicates...),
		// clone intermediate query.
		sql:    hsq.sql.Clone(),
		path:   hsq.path,
		unique: hsq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HairState.Query().
//		GroupBy(hairstate.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hsq *HairStateQuery) GroupBy(field string, fields ...string) *HairStateGroupBy {
	grbuild := &HairStateGroupBy{config: hsq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := hsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return hsq.sqlQuery(ctx), nil
	}
	grbuild.label = hairstate.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.HairState.Query().
//		Select(hairstate.FieldName).
//		Scan(ctx, &v)
func (hsq *HairStateQuery) Select(fields ...string) *HairStateSelect {
	hsq.fields = append(hsq.fields, fields...)
	selbuild := &HairStateSelect{HairStateQuery: hsq}
	selbuild.label = hairstate.Label
	selbuild.flds, selbuild.scan = &hsq.fields, selbuild.Scan
	return selbuild
}

func (hsq *HairStateQuery) prepareQuery(ctx context.Context) error {
	for _, f := range hsq.fields {
		if !hairstate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hsq.path != nil {
		prev, err := hsq.path(ctx)
		if err != nil {
			return err
		}
		hsq.sql = prev
	}
	return nil
}

func (hsq *HairStateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HairState, error) {
	var (
		nodes = []*HairState{}
		_spec = hsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*HairState).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &HairState{config: hsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (hsq *HairStateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hsq.querySpec()
	_spec.Node.Columns = hsq.fields
	if len(hsq.fields) > 0 {
		_spec.Unique = hsq.unique != nil && *hsq.unique
	}
	return sqlgraph.CountNodes(ctx, hsq.driver, _spec)
}

func (hsq *HairStateQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := hsq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (hsq *HairStateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   hairstate.Table,
			Columns: hairstate.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: hairstate.FieldID,
			},
		},
		From:   hsq.sql,
		Unique: true,
	}
	if unique := hsq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := hsq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, hairstate.FieldID)
		for i := range fields {
			if fields[i] != hairstate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := hsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hsq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hsq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hsq *HairStateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hsq.driver.Dialect())
	t1 := builder.Table(hairstate.Table)
	columns := hsq.fields
	if len(columns) == 0 {
		columns = hairstate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hsq.sql != nil {
		selector = hsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hsq.unique != nil && *hsq.unique {
		selector.Distinct()
	}
	for _, p := range hsq.predicates {
		p(selector)
	}
	for _, p := range hsq.order {
		p(selector)
	}
	if offset := hsq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hsq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HairStateGroupBy is the group-by builder for HairState entities.
type HairStateGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hsgb *HairStateGroupBy) Aggregate(fns ...AggregateFunc) *HairStateGroupBy {
	hsgb.fns = append(hsgb.fns, fns...)
	return hsgb
}

// Scan applies the group-by query and scans the result into the given value.
func (hsgb *HairStateGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := hsgb.path(ctx)
	if err != nil {
		return err
	}
	hsgb.sql = query
	return hsgb.sqlScan(ctx, v)
}

func (hsgb *HairStateGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range hsgb.fields {
		if !hairstate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := hsgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hsgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (hsgb *HairStateGroupBy) sqlQuery() *sql.Selector {
	selector := hsgb.sql.Select()
	aggregation := make([]string, 0, len(hsgb.fns))
	for _, fn := range hsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(hsgb.fields)+len(hsgb.fns))
		for _, f := range hsgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(hsgb.fields...)...)
}

// HairStateSelect is the builder for selecting fields of HairState entities.
type HairStateSelect struct {
	*HairStateQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (hss *HairStateSelect) Scan(ctx context.Context, v interface{}) error {
	if err := hss.prepareQuery(ctx); err != nil {
		return err
	}
	hss.sql = hss.HairStateQuery.sqlQuery(ctx)
	return hss.sqlScan(ctx, v)
}

func (hss *HairStateSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := hss.sql.Query()
	if err := hss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

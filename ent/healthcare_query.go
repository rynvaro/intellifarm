// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/healthcare"
	"cattleai/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// HealthCareQuery is the builder for querying HealthCare entities.
type HealthCareQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.HealthCare
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HealthCareQuery builder.
func (hcq *HealthCareQuery) Where(ps ...predicate.HealthCare) *HealthCareQuery {
	hcq.predicates = append(hcq.predicates, ps...)
	return hcq
}

// Limit adds a limit step to the query.
func (hcq *HealthCareQuery) Limit(limit int) *HealthCareQuery {
	hcq.limit = &limit
	return hcq
}

// Offset adds an offset step to the query.
func (hcq *HealthCareQuery) Offset(offset int) *HealthCareQuery {
	hcq.offset = &offset
	return hcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hcq *HealthCareQuery) Unique(unique bool) *HealthCareQuery {
	hcq.unique = &unique
	return hcq
}

// Order adds an order step to the query.
func (hcq *HealthCareQuery) Order(o ...OrderFunc) *HealthCareQuery {
	hcq.order = append(hcq.order, o...)
	return hcq
}

// First returns the first HealthCare entity from the query.
// Returns a *NotFoundError when no HealthCare was found.
func (hcq *HealthCareQuery) First(ctx context.Context) (*HealthCare, error) {
	nodes, err := hcq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{healthcare.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hcq *HealthCareQuery) FirstX(ctx context.Context) *HealthCare {
	node, err := hcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HealthCare ID from the query.
// Returns a *NotFoundError when no HealthCare ID was found.
func (hcq *HealthCareQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hcq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{healthcare.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hcq *HealthCareQuery) FirstIDX(ctx context.Context) int {
	id, err := hcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HealthCare entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HealthCare entity is found.
// Returns a *NotFoundError when no HealthCare entities are found.
func (hcq *HealthCareQuery) Only(ctx context.Context) (*HealthCare, error) {
	nodes, err := hcq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{healthcare.Label}
	default:
		return nil, &NotSingularError{healthcare.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hcq *HealthCareQuery) OnlyX(ctx context.Context) *HealthCare {
	node, err := hcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HealthCare ID in the query.
// Returns a *NotSingularError when more than one HealthCare ID is found.
// Returns a *NotFoundError when no entities are found.
func (hcq *HealthCareQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hcq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{healthcare.Label}
	default:
		err = &NotSingularError{healthcare.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hcq *HealthCareQuery) OnlyIDX(ctx context.Context) int {
	id, err := hcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HealthCares.
func (hcq *HealthCareQuery) All(ctx context.Context) ([]*HealthCare, error) {
	if err := hcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return hcq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (hcq *HealthCareQuery) AllX(ctx context.Context) []*HealthCare {
	nodes, err := hcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HealthCare IDs.
func (hcq *HealthCareQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := hcq.Select(healthcare.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hcq *HealthCareQuery) IDsX(ctx context.Context) []int {
	ids, err := hcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hcq *HealthCareQuery) Count(ctx context.Context) (int, error) {
	if err := hcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return hcq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (hcq *HealthCareQuery) CountX(ctx context.Context) int {
	count, err := hcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hcq *HealthCareQuery) Exist(ctx context.Context) (bool, error) {
	if err := hcq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return hcq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (hcq *HealthCareQuery) ExistX(ctx context.Context) bool {
	exist, err := hcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HealthCareQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hcq *HealthCareQuery) Clone() *HealthCareQuery {
	if hcq == nil {
		return nil
	}
	return &HealthCareQuery{
		config:     hcq.config,
		limit:      hcq.limit,
		offset:     hcq.offset,
		order:      append([]OrderFunc{}, hcq.order...),
		predicates: append([]predicate.HealthCare{}, hcq.predicates...),
		// clone intermediate query.
		sql:    hcq.sql.Clone(),
		path:   hcq.path,
		unique: hcq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CattleId int64 `json:"cattleId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HealthCare.Query().
//		GroupBy(healthcare.FieldCattleId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hcq *HealthCareQuery) GroupBy(field string, fields ...string) *HealthCareGroupBy {
	grbuild := &HealthCareGroupBy{config: hcq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := hcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return hcq.sqlQuery(ctx), nil
	}
	grbuild.label = healthcare.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CattleId int64 `json:"cattleId,omitempty"`
//	}
//
//	client.HealthCare.Query().
//		Select(healthcare.FieldCattleId).
//		Scan(ctx, &v)
func (hcq *HealthCareQuery) Select(fields ...string) *HealthCareSelect {
	hcq.fields = append(hcq.fields, fields...)
	selbuild := &HealthCareSelect{HealthCareQuery: hcq}
	selbuild.label = healthcare.Label
	selbuild.flds, selbuild.scan = &hcq.fields, selbuild.Scan
	return selbuild
}

func (hcq *HealthCareQuery) prepareQuery(ctx context.Context) error {
	for _, f := range hcq.fields {
		if !healthcare.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hcq.path != nil {
		prev, err := hcq.path(ctx)
		if err != nil {
			return err
		}
		hcq.sql = prev
	}
	return nil
}

func (hcq *HealthCareQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HealthCare, error) {
	var (
		nodes = []*HealthCare{}
		_spec = hcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*HealthCare).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &HealthCare{config: hcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (hcq *HealthCareQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hcq.querySpec()
	_spec.Node.Columns = hcq.fields
	if len(hcq.fields) > 0 {
		_spec.Unique = hcq.unique != nil && *hcq.unique
	}
	return sqlgraph.CountNodes(ctx, hcq.driver, _spec)
}

func (hcq *HealthCareQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := hcq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (hcq *HealthCareQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   healthcare.Table,
			Columns: healthcare.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: healthcare.FieldID,
			},
		},
		From:   hcq.sql,
		Unique: true,
	}
	if unique := hcq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := hcq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, healthcare.FieldID)
		for i := range fields {
			if fields[i] != healthcare.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := hcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hcq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hcq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hcq *HealthCareQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hcq.driver.Dialect())
	t1 := builder.Table(healthcare.Table)
	columns := hcq.fields
	if len(columns) == 0 {
		columns = healthcare.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hcq.sql != nil {
		selector = hcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hcq.unique != nil && *hcq.unique {
		selector.Distinct()
	}
	for _, p := range hcq.predicates {
		p(selector)
	}
	for _, p := range hcq.order {
		p(selector)
	}
	if offset := hcq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hcq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HealthCareGroupBy is the group-by builder for HealthCare entities.
type HealthCareGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hcgb *HealthCareGroupBy) Aggregate(fns ...AggregateFunc) *HealthCareGroupBy {
	hcgb.fns = append(hcgb.fns, fns...)
	return hcgb
}

// Scan applies the group-by query and scans the result into the given value.
func (hcgb *HealthCareGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := hcgb.path(ctx)
	if err != nil {
		return err
	}
	hcgb.sql = query
	return hcgb.sqlScan(ctx, v)
}

func (hcgb *HealthCareGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range hcgb.fields {
		if !healthcare.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := hcgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hcgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (hcgb *HealthCareGroupBy) sqlQuery() *sql.Selector {
	selector := hcgb.sql.Select()
	aggregation := make([]string, 0, len(hcgb.fns))
	for _, fn := range hcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(hcgb.fields)+len(hcgb.fns))
		for _, f := range hcgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(hcgb.fields...)...)
}

// HealthCareSelect is the builder for selecting fields of HealthCare entities.
type HealthCareSelect struct {
	*HealthCareQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (hcs *HealthCareSelect) Scan(ctx context.Context, v interface{}) error {
	if err := hcs.prepareQuery(ctx); err != nil {
		return err
	}
	hcs.sql = hcs.HealthCareQuery.sqlQuery(ctx)
	return hcs.sqlScan(ctx, v)
}

func (hcs *HealthCareSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := hcs.sql.Query()
	if err := hcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

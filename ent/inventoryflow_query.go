// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/inventoryflow"
	"cattleai/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// InventoryFlowQuery is the builder for querying InventoryFlow entities.
type InventoryFlowQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.InventoryFlow
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the InventoryFlowQuery builder.
func (ifq *InventoryFlowQuery) Where(ps ...predicate.InventoryFlow) *InventoryFlowQuery {
	ifq.predicates = append(ifq.predicates, ps...)
	return ifq
}

// Limit adds a limit step to the query.
func (ifq *InventoryFlowQuery) Limit(limit int) *InventoryFlowQuery {
	ifq.limit = &limit
	return ifq
}

// Offset adds an offset step to the query.
func (ifq *InventoryFlowQuery) Offset(offset int) *InventoryFlowQuery {
	ifq.offset = &offset
	return ifq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ifq *InventoryFlowQuery) Unique(unique bool) *InventoryFlowQuery {
	ifq.unique = &unique
	return ifq
}

// Order adds an order step to the query.
func (ifq *InventoryFlowQuery) Order(o ...OrderFunc) *InventoryFlowQuery {
	ifq.order = append(ifq.order, o...)
	return ifq
}

// First returns the first InventoryFlow entity from the query.
// Returns a *NotFoundError when no InventoryFlow was found.
func (ifq *InventoryFlowQuery) First(ctx context.Context) (*InventoryFlow, error) {
	nodes, err := ifq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{inventoryflow.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ifq *InventoryFlowQuery) FirstX(ctx context.Context) *InventoryFlow {
	node, err := ifq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first InventoryFlow ID from the query.
// Returns a *NotFoundError when no InventoryFlow ID was found.
func (ifq *InventoryFlowQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ifq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{inventoryflow.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ifq *InventoryFlowQuery) FirstIDX(ctx context.Context) int {
	id, err := ifq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single InventoryFlow entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one InventoryFlow entity is found.
// Returns a *NotFoundError when no InventoryFlow entities are found.
func (ifq *InventoryFlowQuery) Only(ctx context.Context) (*InventoryFlow, error) {
	nodes, err := ifq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{inventoryflow.Label}
	default:
		return nil, &NotSingularError{inventoryflow.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ifq *InventoryFlowQuery) OnlyX(ctx context.Context) *InventoryFlow {
	node, err := ifq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only InventoryFlow ID in the query.
// Returns a *NotSingularError when more than one InventoryFlow ID is found.
// Returns a *NotFoundError when no entities are found.
func (ifq *InventoryFlowQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ifq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{inventoryflow.Label}
	default:
		err = &NotSingularError{inventoryflow.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ifq *InventoryFlowQuery) OnlyIDX(ctx context.Context) int {
	id, err := ifq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of InventoryFlows.
func (ifq *InventoryFlowQuery) All(ctx context.Context) ([]*InventoryFlow, error) {
	if err := ifq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return ifq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (ifq *InventoryFlowQuery) AllX(ctx context.Context) []*InventoryFlow {
	nodes, err := ifq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of InventoryFlow IDs.
func (ifq *InventoryFlowQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := ifq.Select(inventoryflow.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ifq *InventoryFlowQuery) IDsX(ctx context.Context) []int {
	ids, err := ifq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ifq *InventoryFlowQuery) Count(ctx context.Context) (int, error) {
	if err := ifq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return ifq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (ifq *InventoryFlowQuery) CountX(ctx context.Context) int {
	count, err := ifq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ifq *InventoryFlowQuery) Exist(ctx context.Context) (bool, error) {
	if err := ifq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return ifq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (ifq *InventoryFlowQuery) ExistX(ctx context.Context) bool {
	exist, err := ifq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the InventoryFlowQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ifq *InventoryFlowQuery) Clone() *InventoryFlowQuery {
	if ifq == nil {
		return nil
	}
	return &InventoryFlowQuery{
		config:     ifq.config,
		limit:      ifq.limit,
		offset:     ifq.offset,
		order:      append([]OrderFunc{}, ifq.order...),
		predicates: append([]predicate.InventoryFlow{}, ifq.predicates...),
		// clone intermediate query.
		sql:    ifq.sql.Clone(),
		path:   ifq.path,
		unique: ifq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SysMaterialId int64 `json:"sysMaterialId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.InventoryFlow.Query().
//		GroupBy(inventoryflow.FieldSysMaterialId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ifq *InventoryFlowQuery) GroupBy(field string, fields ...string) *InventoryFlowGroupBy {
	grbuild := &InventoryFlowGroupBy{config: ifq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := ifq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return ifq.sqlQuery(ctx), nil
	}
	grbuild.label = inventoryflow.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SysMaterialId int64 `json:"sysMaterialId,omitempty"`
//	}
//
//	client.InventoryFlow.Query().
//		Select(inventoryflow.FieldSysMaterialId).
//		Scan(ctx, &v)
func (ifq *InventoryFlowQuery) Select(fields ...string) *InventoryFlowSelect {
	ifq.fields = append(ifq.fields, fields...)
	selbuild := &InventoryFlowSelect{InventoryFlowQuery: ifq}
	selbuild.label = inventoryflow.Label
	selbuild.flds, selbuild.scan = &ifq.fields, selbuild.Scan
	return selbuild
}

func (ifq *InventoryFlowQuery) prepareQuery(ctx context.Context) error {
	for _, f := range ifq.fields {
		if !inventoryflow.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ifq.path != nil {
		prev, err := ifq.path(ctx)
		if err != nil {
			return err
		}
		ifq.sql = prev
	}
	return nil
}

func (ifq *InventoryFlowQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*InventoryFlow, error) {
	var (
		nodes = []*InventoryFlow{}
		_spec = ifq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*InventoryFlow).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &InventoryFlow{config: ifq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ifq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ifq *InventoryFlowQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ifq.querySpec()
	_spec.Node.Columns = ifq.fields
	if len(ifq.fields) > 0 {
		_spec.Unique = ifq.unique != nil && *ifq.unique
	}
	return sqlgraph.CountNodes(ctx, ifq.driver, _spec)
}

func (ifq *InventoryFlowQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := ifq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (ifq *InventoryFlowQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   inventoryflow.Table,
			Columns: inventoryflow.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: inventoryflow.FieldID,
			},
		},
		From:   ifq.sql,
		Unique: true,
	}
	if unique := ifq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := ifq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, inventoryflow.FieldID)
		for i := range fields {
			if fields[i] != inventoryflow.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ifq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ifq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ifq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ifq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ifq *InventoryFlowQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ifq.driver.Dialect())
	t1 := builder.Table(inventoryflow.Table)
	columns := ifq.fields
	if len(columns) == 0 {
		columns = inventoryflow.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ifq.sql != nil {
		selector = ifq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ifq.unique != nil && *ifq.unique {
		selector.Distinct()
	}
	for _, p := range ifq.predicates {
		p(selector)
	}
	for _, p := range ifq.order {
		p(selector)
	}
	if offset := ifq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ifq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// InventoryFlowGroupBy is the group-by builder for InventoryFlow entities.
type InventoryFlowGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ifgb *InventoryFlowGroupBy) Aggregate(fns ...AggregateFunc) *InventoryFlowGroupBy {
	ifgb.fns = append(ifgb.fns, fns...)
	return ifgb
}

// Scan applies the group-by query and scans the result into the given value.
func (ifgb *InventoryFlowGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := ifgb.path(ctx)
	if err != nil {
		return err
	}
	ifgb.sql = query
	return ifgb.sqlScan(ctx, v)
}

func (ifgb *InventoryFlowGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range ifgb.fields {
		if !inventoryflow.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := ifgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ifgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (ifgb *InventoryFlowGroupBy) sqlQuery() *sql.Selector {
	selector := ifgb.sql.Select()
	aggregation := make([]string, 0, len(ifgb.fns))
	for _, fn := range ifgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(ifgb.fields)+len(ifgb.fns))
		for _, f := range ifgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(ifgb.fields...)...)
}

// InventoryFlowSelect is the builder for selecting fields of InventoryFlow entities.
type InventoryFlowSelect struct {
	*InventoryFlowQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (ifs *InventoryFlowSelect) Scan(ctx context.Context, v interface{}) error {
	if err := ifs.prepareQuery(ctx); err != nil {
		return err
	}
	ifs.sql = ifs.InventoryFlowQuery.sqlQuery(ctx)
	return ifs.sqlScan(ctx, v)
}

func (ifs *InventoryFlowSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := ifs.sql.Query()
	if err := ifs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

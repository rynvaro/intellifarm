// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/positionapi"
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// PositionApi is the model entity for the PositionApi schema.
type PositionApi struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 职务ID
	PositionId int64 `json:"positionId,omitempty"`
	// api ID 列表，逗号分割
	Apis string `json:"apis,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PositionApi) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case positionapi.FieldID, positionapi.FieldPositionId:
			values[i] = new(sql.NullInt64)
		case positionapi.FieldApis:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type PositionApi", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PositionApi fields.
func (pa *PositionApi) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case positionapi.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pa.ID = int(value.Int64)
		case positionapi.FieldPositionId:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field positionId", values[i])
			} else if value.Valid {
				pa.PositionId = value.Int64
			}
		case positionapi.FieldApis:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field apis", values[i])
			} else if value.Valid {
				pa.Apis = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this PositionApi.
// Note that you need to call PositionApi.Unwrap() before calling this method if this PositionApi
// was returned from a transaction, and the transaction was committed or rolled back.
func (pa *PositionApi) Update() *PositionApiUpdateOne {
	return (&PositionApiClient{config: pa.config}).UpdateOne(pa)
}

// Unwrap unwraps the PositionApi entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pa *PositionApi) Unwrap() *PositionApi {
	_tx, ok := pa.config.driver.(*txDriver)
	if !ok {
		panic("ent: PositionApi is not a transactional entity")
	}
	pa.config.driver = _tx.drv
	return pa
}

// String implements the fmt.Stringer.
func (pa *PositionApi) String() string {
	var builder strings.Builder
	builder.WriteString("PositionApi(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pa.ID))
	builder.WriteString("positionId=")
	builder.WriteString(fmt.Sprintf("%v", pa.PositionId))
	builder.WriteString(", ")
	builder.WriteString("apis=")
	builder.WriteString(pa.Apis)
	builder.WriteByte(')')
	return builder.String()
}

// PositionApis is a parsable slice of PositionApi.
type PositionApis []*PositionApi

func (pa PositionApis) config(cfg config) {
	for _i := range pa {
		pa[_i].config = cfg
	}
}

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/positionapi"
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PositionApiCreate is the builder for creating a PositionApi entity.
type PositionApiCreate struct {
	config
	mutation *PositionApiMutation
	hooks    []Hook
}

// SetPositionId sets the "positionId" field.
func (pac *PositionApiCreate) SetPositionId(i int64) *PositionApiCreate {
	pac.mutation.SetPositionId(i)
	return pac
}

// SetApis sets the "apis" field.
func (pac *PositionApiCreate) SetApis(s string) *PositionApiCreate {
	pac.mutation.SetApis(s)
	return pac
}

// Mutation returns the PositionApiMutation object of the builder.
func (pac *PositionApiCreate) Mutation() *PositionApiMutation {
	return pac.mutation
}

// Save creates the PositionApi in the database.
func (pac *PositionApiCreate) Save(ctx context.Context) (*PositionApi, error) {
	var (
		err  error
		node *PositionApi
	)
	if len(pac.hooks) == 0 {
		if err = pac.check(); err != nil {
			return nil, err
		}
		node, err = pac.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PositionApiMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pac.check(); err != nil {
				return nil, err
			}
			pac.mutation = mutation
			if node, err = pac.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(pac.hooks) - 1; i >= 0; i-- {
			if pac.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = pac.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, pac.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*PositionApi)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from PositionApiMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pac *PositionApiCreate) SaveX(ctx context.Context) *PositionApi {
	v, err := pac.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pac *PositionApiCreate) Exec(ctx context.Context) error {
	_, err := pac.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pac *PositionApiCreate) ExecX(ctx context.Context) {
	if err := pac.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pac *PositionApiCreate) check() error {
	if _, ok := pac.mutation.PositionId(); !ok {
		return &ValidationError{Name: "positionId", err: errors.New(`ent: missing required field "PositionApi.positionId"`)}
	}
	if _, ok := pac.mutation.Apis(); !ok {
		return &ValidationError{Name: "apis", err: errors.New(`ent: missing required field "PositionApi.apis"`)}
	}
	return nil
}

func (pac *PositionApiCreate) sqlSave(ctx context.Context) (*PositionApi, error) {
	_node, _spec := pac.createSpec()
	if err := sqlgraph.CreateNode(ctx, pac.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (pac *PositionApiCreate) createSpec() (*PositionApi, *sqlgraph.CreateSpec) {
	var (
		_node = &PositionApi{config: pac.config}
		_spec = &sqlgraph.CreateSpec{
			Table: positionapi.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: positionapi.FieldID,
			},
		}
	)
	if value, ok := pac.mutation.PositionId(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt64,
			Value:  value,
			Column: positionapi.FieldPositionId,
		})
		_node.PositionId = value
	}
	if value, ok := pac.mutation.Apis(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: positionapi.FieldApis,
		})
		_node.Apis = value
	}
	return _node, _spec
}

// PositionApiCreateBulk is the builder for creating many PositionApi entities in bulk.
type PositionApiCreateBulk struct {
	config
	builders []*PositionApiCreate
}

// Save creates the PositionApi entities in the database.
func (pacb *PositionApiCreateBulk) Save(ctx context.Context) ([]*PositionApi, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pacb.builders))
	nodes := make([]*PositionApi, len(pacb.builders))
	mutators := make([]Mutator, len(pacb.builders))
	for i := range pacb.builders {
		func(i int, root context.Context) {
			builder := pacb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PositionApiMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pacb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pacb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pacb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pacb *PositionApiCreateBulk) SaveX(ctx context.Context) []*PositionApi {
	v, err := pacb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pacb *PositionApiCreateBulk) Exec(ctx context.Context) error {
	_, err := pacb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pacb *PositionApiCreateBulk) ExecX(ctx context.Context) {
	if err := pacb.Exec(ctx); err != nil {
		panic(err)
	}
}

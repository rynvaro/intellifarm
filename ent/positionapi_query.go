// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/positionapi"
	"cattleai/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PositionApiQuery is the builder for querying PositionApi entities.
type PositionApiQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.PositionApi
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PositionApiQuery builder.
func (paq *PositionApiQuery) Where(ps ...predicate.PositionApi) *PositionApiQuery {
	paq.predicates = append(paq.predicates, ps...)
	return paq
}

// Limit adds a limit step to the query.
func (paq *PositionApiQuery) Limit(limit int) *PositionApiQuery {
	paq.limit = &limit
	return paq
}

// Offset adds an offset step to the query.
func (paq *PositionApiQuery) Offset(offset int) *PositionApiQuery {
	paq.offset = &offset
	return paq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (paq *PositionApiQuery) Unique(unique bool) *PositionApiQuery {
	paq.unique = &unique
	return paq
}

// Order adds an order step to the query.
func (paq *PositionApiQuery) Order(o ...OrderFunc) *PositionApiQuery {
	paq.order = append(paq.order, o...)
	return paq
}

// First returns the first PositionApi entity from the query.
// Returns a *NotFoundError when no PositionApi was found.
func (paq *PositionApiQuery) First(ctx context.Context) (*PositionApi, error) {
	nodes, err := paq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{positionapi.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (paq *PositionApiQuery) FirstX(ctx context.Context) *PositionApi {
	node, err := paq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PositionApi ID from the query.
// Returns a *NotFoundError when no PositionApi ID was found.
func (paq *PositionApiQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = paq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{positionapi.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (paq *PositionApiQuery) FirstIDX(ctx context.Context) int {
	id, err := paq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PositionApi entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PositionApi entity is found.
// Returns a *NotFoundError when no PositionApi entities are found.
func (paq *PositionApiQuery) Only(ctx context.Context) (*PositionApi, error) {
	nodes, err := paq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{positionapi.Label}
	default:
		return nil, &NotSingularError{positionapi.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (paq *PositionApiQuery) OnlyX(ctx context.Context) *PositionApi {
	node, err := paq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PositionApi ID in the query.
// Returns a *NotSingularError when more than one PositionApi ID is found.
// Returns a *NotFoundError when no entities are found.
func (paq *PositionApiQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = paq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{positionapi.Label}
	default:
		err = &NotSingularError{positionapi.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (paq *PositionApiQuery) OnlyIDX(ctx context.Context) int {
	id, err := paq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PositionApis.
func (paq *PositionApiQuery) All(ctx context.Context) ([]*PositionApi, error) {
	if err := paq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return paq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (paq *PositionApiQuery) AllX(ctx context.Context) []*PositionApi {
	nodes, err := paq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PositionApi IDs.
func (paq *PositionApiQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := paq.Select(positionapi.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (paq *PositionApiQuery) IDsX(ctx context.Context) []int {
	ids, err := paq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (paq *PositionApiQuery) Count(ctx context.Context) (int, error) {
	if err := paq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return paq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (paq *PositionApiQuery) CountX(ctx context.Context) int {
	count, err := paq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (paq *PositionApiQuery) Exist(ctx context.Context) (bool, error) {
	if err := paq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return paq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (paq *PositionApiQuery) ExistX(ctx context.Context) bool {
	exist, err := paq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PositionApiQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (paq *PositionApiQuery) Clone() *PositionApiQuery {
	if paq == nil {
		return nil
	}
	return &PositionApiQuery{
		config:     paq.config,
		limit:      paq.limit,
		offset:     paq.offset,
		order:      append([]OrderFunc{}, paq.order...),
		predicates: append([]predicate.PositionApi{}, paq.predicates...),
		// clone intermediate query.
		sql:    paq.sql.Clone(),
		path:   paq.path,
		unique: paq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PositionId int64 `json:"positionId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PositionApi.Query().
//		GroupBy(positionapi.FieldPositionId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (paq *PositionApiQuery) GroupBy(field string, fields ...string) *PositionApiGroupBy {
	grbuild := &PositionApiGroupBy{config: paq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := paq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return paq.sqlQuery(ctx), nil
	}
	grbuild.label = positionapi.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PositionId int64 `json:"positionId,omitempty"`
//	}
//
//	client.PositionApi.Query().
//		Select(positionapi.FieldPositionId).
//		Scan(ctx, &v)
func (paq *PositionApiQuery) Select(fields ...string) *PositionApiSelect {
	paq.fields = append(paq.fields, fields...)
	selbuild := &PositionApiSelect{PositionApiQuery: paq}
	selbuild.label = positionapi.Label
	selbuild.flds, selbuild.scan = &paq.fields, selbuild.Scan
	return selbuild
}

func (paq *PositionApiQuery) prepareQuery(ctx context.Context) error {
	for _, f := range paq.fields {
		if !positionapi.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if paq.path != nil {
		prev, err := paq.path(ctx)
		if err != nil {
			return err
		}
		paq.sql = prev
	}
	return nil
}

func (paq *PositionApiQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PositionApi, error) {
	var (
		nodes = []*PositionApi{}
		_spec = paq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*PositionApi).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &PositionApi{config: paq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, paq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (paq *PositionApiQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := paq.querySpec()
	_spec.Node.Columns = paq.fields
	if len(paq.fields) > 0 {
		_spec.Unique = paq.unique != nil && *paq.unique
	}
	return sqlgraph.CountNodes(ctx, paq.driver, _spec)
}

func (paq *PositionApiQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := paq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (paq *PositionApiQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   positionapi.Table,
			Columns: positionapi.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: positionapi.FieldID,
			},
		},
		From:   paq.sql,
		Unique: true,
	}
	if unique := paq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := paq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, positionapi.FieldID)
		for i := range fields {
			if fields[i] != positionapi.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := paq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := paq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := paq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := paq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (paq *PositionApiQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(paq.driver.Dialect())
	t1 := builder.Table(positionapi.Table)
	columns := paq.fields
	if len(columns) == 0 {
		columns = positionapi.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if paq.sql != nil {
		selector = paq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if paq.unique != nil && *paq.unique {
		selector.Distinct()
	}
	for _, p := range paq.predicates {
		p(selector)
	}
	for _, p := range paq.order {
		p(selector)
	}
	if offset := paq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := paq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PositionApiGroupBy is the group-by builder for PositionApi entities.
type PositionApiGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pagb *PositionApiGroupBy) Aggregate(fns ...AggregateFunc) *PositionApiGroupBy {
	pagb.fns = append(pagb.fns, fns...)
	return pagb
}

// Scan applies the group-by query and scans the result into the given value.
func (pagb *PositionApiGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := pagb.path(ctx)
	if err != nil {
		return err
	}
	pagb.sql = query
	return pagb.sqlScan(ctx, v)
}

func (pagb *PositionApiGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range pagb.fields {
		if !positionapi.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := pagb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pagb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pagb *PositionApiGroupBy) sqlQuery() *sql.Selector {
	selector := pagb.sql.Select()
	aggregation := make([]string, 0, len(pagb.fns))
	for _, fn := range pagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(pagb.fields)+len(pagb.fns))
		for _, f := range pagb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(pagb.fields...)...)
}

// PositionApiSelect is the builder for selecting fields of PositionApi entities.
type PositionApiSelect struct {
	*PositionApiQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (pas *PositionApiSelect) Scan(ctx context.Context, v interface{}) error {
	if err := pas.prepareQuery(ctx); err != nil {
		return err
	}
	pas.sql = pas.PositionApiQuery.sqlQuery(ctx)
	return pas.sqlScan(ctx, v)
}

func (pas *PositionApiSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := pas.sql.Query()
	if err := pas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

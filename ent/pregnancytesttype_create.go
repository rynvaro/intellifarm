// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/pregnancytesttype"
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
)

// PregnancyTestTypeCreate is the builder for creating a PregnancyTestType entity.
type PregnancyTestTypeCreate struct {
	config
	mutation *PregnancyTestTypeMutation
	hooks    []Hook
}

// SetName sets the name field.
func (pttc *PregnancyTestTypeCreate) SetName(s string) *PregnancyTestTypeCreate {
	pttc.mutation.SetName(s)
	return pttc
}

// Mutation returns the PregnancyTestTypeMutation object of the builder.
func (pttc *PregnancyTestTypeCreate) Mutation() *PregnancyTestTypeMutation {
	return pttc.mutation
}

// Save creates the PregnancyTestType in the database.
func (pttc *PregnancyTestTypeCreate) Save(ctx context.Context) (*PregnancyTestType, error) {
	var (
		err  error
		node *PregnancyTestType
	)
	if len(pttc.hooks) == 0 {
		if err = pttc.check(); err != nil {
			return nil, err
		}
		node, err = pttc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PregnancyTestTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pttc.check(); err != nil {
				return nil, err
			}
			pttc.mutation = mutation
			node, err = pttc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(pttc.hooks) - 1; i >= 0; i-- {
			mut = pttc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pttc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pttc *PregnancyTestTypeCreate) SaveX(ctx context.Context) *PregnancyTestType {
	v, err := pttc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (pttc *PregnancyTestTypeCreate) check() error {
	if _, ok := pttc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := pttc.mutation.Name(); ok {
		if err := pregnancytesttype.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (pttc *PregnancyTestTypeCreate) sqlSave(ctx context.Context) (*PregnancyTestType, error) {
	_node, _spec := pttc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pttc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int64(id)
	return _node, nil
}

func (pttc *PregnancyTestTypeCreate) createSpec() (*PregnancyTestType, *sqlgraph.CreateSpec) {
	var (
		_node = &PregnancyTestType{config: pttc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: pregnancytesttype.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: pregnancytesttype.FieldID,
			},
		}
	)
	if value, ok := pttc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: pregnancytesttype.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// PregnancyTestTypeCreateBulk is the builder for creating a bulk of PregnancyTestType entities.
type PregnancyTestTypeCreateBulk struct {
	config
	builders []*PregnancyTestTypeCreate
}

// Save creates the PregnancyTestType entities in the database.
func (pttcb *PregnancyTestTypeCreateBulk) Save(ctx context.Context) ([]*PregnancyTestType, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pttcb.builders))
	nodes := make([]*PregnancyTestType, len(pttcb.builders))
	mutators := make([]Mutator, len(pttcb.builders))
	for i := range pttcb.builders {
		func(i int, root context.Context) {
			builder := pttcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PregnancyTestTypeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pttcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pttcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int64(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pttcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (pttcb *PregnancyTestTypeCreateBulk) SaveX(ctx context.Context) []*PregnancyTestType {
	v, err := pttcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

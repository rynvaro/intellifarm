// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/reproductivestate"
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
)

// ReproductiveStateCreate is the builder for creating a ReproductiveState entity.
type ReproductiveStateCreate struct {
	config
	mutation *ReproductiveStateMutation
	hooks    []Hook
}

// SetName sets the name field.
func (rsc *ReproductiveStateCreate) SetName(s string) *ReproductiveStateCreate {
	rsc.mutation.SetName(s)
	return rsc
}

// Mutation returns the ReproductiveStateMutation object of the builder.
func (rsc *ReproductiveStateCreate) Mutation() *ReproductiveStateMutation {
	return rsc.mutation
}

// Save creates the ReproductiveState in the database.
func (rsc *ReproductiveStateCreate) Save(ctx context.Context) (*ReproductiveState, error) {
	var (
		err  error
		node *ReproductiveState
	)
	if len(rsc.hooks) == 0 {
		if err = rsc.check(); err != nil {
			return nil, err
		}
		node, err = rsc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ReproductiveStateMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = rsc.check(); err != nil {
				return nil, err
			}
			rsc.mutation = mutation
			node, err = rsc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(rsc.hooks) - 1; i >= 0; i-- {
			mut = rsc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, rsc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (rsc *ReproductiveStateCreate) SaveX(ctx context.Context) *ReproductiveState {
	v, err := rsc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (rsc *ReproductiveStateCreate) check() error {
	if _, ok := rsc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := rsc.mutation.Name(); ok {
		if err := reproductivestate.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (rsc *ReproductiveStateCreate) sqlSave(ctx context.Context) (*ReproductiveState, error) {
	_node, _spec := rsc.createSpec()
	if err := sqlgraph.CreateNode(ctx, rsc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int64(id)
	return _node, nil
}

func (rsc *ReproductiveStateCreate) createSpec() (*ReproductiveState, *sqlgraph.CreateSpec) {
	var (
		_node = &ReproductiveState{config: rsc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: reproductivestate.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: reproductivestate.FieldID,
			},
		}
	)
	if value, ok := rsc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: reproductivestate.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// ReproductiveStateCreateBulk is the builder for creating a bulk of ReproductiveState entities.
type ReproductiveStateCreateBulk struct {
	config
	builders []*ReproductiveStateCreate
}

// Save creates the ReproductiveState entities in the database.
func (rscb *ReproductiveStateCreateBulk) Save(ctx context.Context) ([]*ReproductiveState, error) {
	specs := make([]*sqlgraph.CreateSpec, len(rscb.builders))
	nodes := make([]*ReproductiveState, len(rscb.builders))
	mutators := make([]Mutator, len(rscb.builders))
	for i := range rscb.builders {
		func(i int, root context.Context) {
			builder := rscb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ReproductiveStateMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, rscb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, rscb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int64(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, rscb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (rscb *ReproductiveStateCreateBulk) SaveX(ctx context.Context) []*ReproductiveState {
	v, err := rscb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

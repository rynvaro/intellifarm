// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/predicate"
	"cattleai/ent/reproductivestate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ReproductiveStateQuery is the builder for querying ReproductiveState entities.
type ReproductiveStateQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ReproductiveState
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ReproductiveStateQuery builder.
func (rsq *ReproductiveStateQuery) Where(ps ...predicate.ReproductiveState) *ReproductiveStateQuery {
	rsq.predicates = append(rsq.predicates, ps...)
	return rsq
}

// Limit adds a limit step to the query.
func (rsq *ReproductiveStateQuery) Limit(limit int) *ReproductiveStateQuery {
	rsq.limit = &limit
	return rsq
}

// Offset adds an offset step to the query.
func (rsq *ReproductiveStateQuery) Offset(offset int) *ReproductiveStateQuery {
	rsq.offset = &offset
	return rsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rsq *ReproductiveStateQuery) Unique(unique bool) *ReproductiveStateQuery {
	rsq.unique = &unique
	return rsq
}

// Order adds an order step to the query.
func (rsq *ReproductiveStateQuery) Order(o ...OrderFunc) *ReproductiveStateQuery {
	rsq.order = append(rsq.order, o...)
	return rsq
}

// First returns the first ReproductiveState entity from the query.
// Returns a *NotFoundError when no ReproductiveState was found.
func (rsq *ReproductiveStateQuery) First(ctx context.Context) (*ReproductiveState, error) {
	nodes, err := rsq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{reproductivestate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rsq *ReproductiveStateQuery) FirstX(ctx context.Context) *ReproductiveState {
	node, err := rsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ReproductiveState ID from the query.
// Returns a *NotFoundError when no ReproductiveState ID was found.
func (rsq *ReproductiveStateQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rsq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{reproductivestate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rsq *ReproductiveStateQuery) FirstIDX(ctx context.Context) int {
	id, err := rsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ReproductiveState entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ReproductiveState entity is found.
// Returns a *NotFoundError when no ReproductiveState entities are found.
func (rsq *ReproductiveStateQuery) Only(ctx context.Context) (*ReproductiveState, error) {
	nodes, err := rsq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{reproductivestate.Label}
	default:
		return nil, &NotSingularError{reproductivestate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rsq *ReproductiveStateQuery) OnlyX(ctx context.Context) *ReproductiveState {
	node, err := rsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ReproductiveState ID in the query.
// Returns a *NotSingularError when more than one ReproductiveState ID is found.
// Returns a *NotFoundError when no entities are found.
func (rsq *ReproductiveStateQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rsq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{reproductivestate.Label}
	default:
		err = &NotSingularError{reproductivestate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rsq *ReproductiveStateQuery) OnlyIDX(ctx context.Context) int {
	id, err := rsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ReproductiveStates.
func (rsq *ReproductiveStateQuery) All(ctx context.Context) ([]*ReproductiveState, error) {
	if err := rsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return rsq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (rsq *ReproductiveStateQuery) AllX(ctx context.Context) []*ReproductiveState {
	nodes, err := rsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ReproductiveState IDs.
func (rsq *ReproductiveStateQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := rsq.Select(reproductivestate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rsq *ReproductiveStateQuery) IDsX(ctx context.Context) []int {
	ids, err := rsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rsq *ReproductiveStateQuery) Count(ctx context.Context) (int, error) {
	if err := rsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return rsq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (rsq *ReproductiveStateQuery) CountX(ctx context.Context) int {
	count, err := rsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rsq *ReproductiveStateQuery) Exist(ctx context.Context) (bool, error) {
	if err := rsq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return rsq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (rsq *ReproductiveStateQuery) ExistX(ctx context.Context) bool {
	exist, err := rsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ReproductiveStateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rsq *ReproductiveStateQuery) Clone() *ReproductiveStateQuery {
	if rsq == nil {
		return nil
	}
	return &ReproductiveStateQuery{
		config:     rsq.config,
		limit:      rsq.limit,
		offset:     rsq.offset,
		order:      append([]OrderFunc{}, rsq.order...),
		predicates: append([]predicate.ReproductiveState{}, rsq.predicates...),
		// clone intermediate query.
		sql:    rsq.sql.Clone(),
		path:   rsq.path,
		unique: rsq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ReproductiveState.Query().
//		GroupBy(reproductivestate.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rsq *ReproductiveStateQuery) GroupBy(field string, fields ...string) *ReproductiveStateGroupBy {
	grbuild := &ReproductiveStateGroupBy{config: rsq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := rsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return rsq.sqlQuery(ctx), nil
	}
	grbuild.label = reproductivestate.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.ReproductiveState.Query().
//		Select(reproductivestate.FieldName).
//		Scan(ctx, &v)
func (rsq *ReproductiveStateQuery) Select(fields ...string) *ReproductiveStateSelect {
	rsq.fields = append(rsq.fields, fields...)
	selbuild := &ReproductiveStateSelect{ReproductiveStateQuery: rsq}
	selbuild.label = reproductivestate.Label
	selbuild.flds, selbuild.scan = &rsq.fields, selbuild.Scan
	return selbuild
}

func (rsq *ReproductiveStateQuery) prepareQuery(ctx context.Context) error {
	for _, f := range rsq.fields {
		if !reproductivestate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rsq.path != nil {
		prev, err := rsq.path(ctx)
		if err != nil {
			return err
		}
		rsq.sql = prev
	}
	return nil
}

func (rsq *ReproductiveStateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ReproductiveState, error) {
	var (
		nodes = []*ReproductiveState{}
		_spec = rsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*ReproductiveState).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &ReproductiveState{config: rsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rsq *ReproductiveStateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rsq.querySpec()
	_spec.Node.Columns = rsq.fields
	if len(rsq.fields) > 0 {
		_spec.Unique = rsq.unique != nil && *rsq.unique
	}
	return sqlgraph.CountNodes(ctx, rsq.driver, _spec)
}

func (rsq *ReproductiveStateQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := rsq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (rsq *ReproductiveStateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   reproductivestate.Table,
			Columns: reproductivestate.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: reproductivestate.FieldID,
			},
		},
		From:   rsq.sql,
		Unique: true,
	}
	if unique := rsq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := rsq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, reproductivestate.FieldID)
		for i := range fields {
			if fields[i] != reproductivestate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rsq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rsq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rsq *ReproductiveStateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rsq.driver.Dialect())
	t1 := builder.Table(reproductivestate.Table)
	columns := rsq.fields
	if len(columns) == 0 {
		columns = reproductivestate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rsq.sql != nil {
		selector = rsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rsq.unique != nil && *rsq.unique {
		selector.Distinct()
	}
	for _, p := range rsq.predicates {
		p(selector)
	}
	for _, p := range rsq.order {
		p(selector)
	}
	if offset := rsq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rsq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ReproductiveStateGroupBy is the group-by builder for ReproductiveState entities.
type ReproductiveStateGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rsgb *ReproductiveStateGroupBy) Aggregate(fns ...AggregateFunc) *ReproductiveStateGroupBy {
	rsgb.fns = append(rsgb.fns, fns...)
	return rsgb
}

// Scan applies the group-by query and scans the result into the given value.
func (rsgb *ReproductiveStateGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := rsgb.path(ctx)
	if err != nil {
		return err
	}
	rsgb.sql = query
	return rsgb.sqlScan(ctx, v)
}

func (rsgb *ReproductiveStateGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range rsgb.fields {
		if !reproductivestate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := rsgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rsgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (rsgb *ReproductiveStateGroupBy) sqlQuery() *sql.Selector {
	selector := rsgb.sql.Select()
	aggregation := make([]string, 0, len(rsgb.fns))
	for _, fn := range rsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(rsgb.fields)+len(rsgb.fns))
		for _, f := range rsgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(rsgb.fields...)...)
}

// ReproductiveStateSelect is the builder for selecting fields of ReproductiveState entities.
type ReproductiveStateSelect struct {
	*ReproductiveStateQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (rss *ReproductiveStateSelect) Scan(ctx context.Context, v interface{}) error {
	if err := rss.prepareQuery(ctx); err != nil {
		return err
	}
	rss.sql = rss.ReproductiveStateQuery.sqlQuery(ctx)
	return rss.sqlScan(ctx, v)
}

func (rss *ReproductiveStateSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := rss.sql.Query()
	if err := rss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

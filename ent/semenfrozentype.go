// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/semenfrozentype"
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// SemenFrozenType is the model entity for the SemenFrozenType schema.
type SemenFrozenType struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 名称
	Name string `json:"name,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SemenFrozenType) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case semenfrozentype.FieldID:
			values[i] = new(sql.NullInt64)
		case semenfrozentype.FieldName:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type SemenFrozenType", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SemenFrozenType fields.
func (sft *SemenFrozenType) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case semenfrozentype.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sft.ID = int(value.Int64)
		case semenfrozentype.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				sft.Name = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this SemenFrozenType.
// Note that you need to call SemenFrozenType.Unwrap() before calling this method if this SemenFrozenType
// was returned from a transaction, and the transaction was committed or rolled back.
func (sft *SemenFrozenType) Update() *SemenFrozenTypeUpdateOne {
	return (&SemenFrozenTypeClient{config: sft.config}).UpdateOne(sft)
}

// Unwrap unwraps the SemenFrozenType entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sft *SemenFrozenType) Unwrap() *SemenFrozenType {
	_tx, ok := sft.config.driver.(*txDriver)
	if !ok {
		panic("ent: SemenFrozenType is not a transactional entity")
	}
	sft.config.driver = _tx.drv
	return sft
}

// String implements the fmt.Stringer.
func (sft *SemenFrozenType) String() string {
	var builder strings.Builder
	builder.WriteString("SemenFrozenType(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sft.ID))
	builder.WriteString("name=")
	builder.WriteString(sft.Name)
	builder.WriteByte(')')
	return builder.String()
}

// SemenFrozenTypes is a parsable slice of SemenFrozenType.
type SemenFrozenTypes []*SemenFrozenType

func (sft SemenFrozenTypes) config(cfg config) {
	for _i := range sft {
		sft[_i].config = cfg
	}
}

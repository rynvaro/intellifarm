// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/semenfrozentype"
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SemenFrozenTypeCreate is the builder for creating a SemenFrozenType entity.
type SemenFrozenTypeCreate struct {
	config
	mutation *SemenFrozenTypeMutation
	hooks    []Hook
}

// SetName sets the "name" field.
func (sftc *SemenFrozenTypeCreate) SetName(s string) *SemenFrozenTypeCreate {
	sftc.mutation.SetName(s)
	return sftc
}

// Mutation returns the SemenFrozenTypeMutation object of the builder.
func (sftc *SemenFrozenTypeCreate) Mutation() *SemenFrozenTypeMutation {
	return sftc.mutation
}

// Save creates the SemenFrozenType in the database.
func (sftc *SemenFrozenTypeCreate) Save(ctx context.Context) (*SemenFrozenType, error) {
	var (
		err  error
		node *SemenFrozenType
	)
	if len(sftc.hooks) == 0 {
		if err = sftc.check(); err != nil {
			return nil, err
		}
		node, err = sftc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SemenFrozenTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = sftc.check(); err != nil {
				return nil, err
			}
			sftc.mutation = mutation
			if node, err = sftc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(sftc.hooks) - 1; i >= 0; i-- {
			if sftc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = sftc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, sftc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*SemenFrozenType)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from SemenFrozenTypeMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (sftc *SemenFrozenTypeCreate) SaveX(ctx context.Context) *SemenFrozenType {
	v, err := sftc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (sftc *SemenFrozenTypeCreate) Exec(ctx context.Context) error {
	_, err := sftc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sftc *SemenFrozenTypeCreate) ExecX(ctx context.Context) {
	if err := sftc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (sftc *SemenFrozenTypeCreate) check() error {
	if _, ok := sftc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "SemenFrozenType.name"`)}
	}
	return nil
}

func (sftc *SemenFrozenTypeCreate) sqlSave(ctx context.Context) (*SemenFrozenType, error) {
	_node, _spec := sftc.createSpec()
	if err := sqlgraph.CreateNode(ctx, sftc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (sftc *SemenFrozenTypeCreate) createSpec() (*SemenFrozenType, *sqlgraph.CreateSpec) {
	var (
		_node = &SemenFrozenType{config: sftc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: semenfrozentype.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: semenfrozentype.FieldID,
			},
		}
	)
	if value, ok := sftc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: semenfrozentype.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// SemenFrozenTypeCreateBulk is the builder for creating many SemenFrozenType entities in bulk.
type SemenFrozenTypeCreateBulk struct {
	config
	builders []*SemenFrozenTypeCreate
}

// Save creates the SemenFrozenType entities in the database.
func (sftcb *SemenFrozenTypeCreateBulk) Save(ctx context.Context) ([]*SemenFrozenType, error) {
	specs := make([]*sqlgraph.CreateSpec, len(sftcb.builders))
	nodes := make([]*SemenFrozenType, len(sftcb.builders))
	mutators := make([]Mutator, len(sftcb.builders))
	for i := range sftcb.builders {
		func(i int, root context.Context) {
			builder := sftcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SemenFrozenTypeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, sftcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, sftcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, sftcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (sftcb *SemenFrozenTypeCreateBulk) SaveX(ctx context.Context) []*SemenFrozenType {
	v, err := sftcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (sftcb *SemenFrozenTypeCreateBulk) Exec(ctx context.Context) error {
	_, err := sftcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sftcb *SemenFrozenTypeCreateBulk) ExecX(ctx context.Context) {
	if err := sftcb.Exec(ctx); err != nil {
		panic(err)
	}
}

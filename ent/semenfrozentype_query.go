// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/predicate"
	"cattleai/ent/semenfrozentype"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SemenFrozenTypeQuery is the builder for querying SemenFrozenType entities.
type SemenFrozenTypeQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.SemenFrozenType
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SemenFrozenTypeQuery builder.
func (sftq *SemenFrozenTypeQuery) Where(ps ...predicate.SemenFrozenType) *SemenFrozenTypeQuery {
	sftq.predicates = append(sftq.predicates, ps...)
	return sftq
}

// Limit adds a limit step to the query.
func (sftq *SemenFrozenTypeQuery) Limit(limit int) *SemenFrozenTypeQuery {
	sftq.limit = &limit
	return sftq
}

// Offset adds an offset step to the query.
func (sftq *SemenFrozenTypeQuery) Offset(offset int) *SemenFrozenTypeQuery {
	sftq.offset = &offset
	return sftq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sftq *SemenFrozenTypeQuery) Unique(unique bool) *SemenFrozenTypeQuery {
	sftq.unique = &unique
	return sftq
}

// Order adds an order step to the query.
func (sftq *SemenFrozenTypeQuery) Order(o ...OrderFunc) *SemenFrozenTypeQuery {
	sftq.order = append(sftq.order, o...)
	return sftq
}

// First returns the first SemenFrozenType entity from the query.
// Returns a *NotFoundError when no SemenFrozenType was found.
func (sftq *SemenFrozenTypeQuery) First(ctx context.Context) (*SemenFrozenType, error) {
	nodes, err := sftq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{semenfrozentype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sftq *SemenFrozenTypeQuery) FirstX(ctx context.Context) *SemenFrozenType {
	node, err := sftq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SemenFrozenType ID from the query.
// Returns a *NotFoundError when no SemenFrozenType ID was found.
func (sftq *SemenFrozenTypeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sftq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{semenfrozentype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sftq *SemenFrozenTypeQuery) FirstIDX(ctx context.Context) int {
	id, err := sftq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SemenFrozenType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SemenFrozenType entity is found.
// Returns a *NotFoundError when no SemenFrozenType entities are found.
func (sftq *SemenFrozenTypeQuery) Only(ctx context.Context) (*SemenFrozenType, error) {
	nodes, err := sftq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{semenfrozentype.Label}
	default:
		return nil, &NotSingularError{semenfrozentype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sftq *SemenFrozenTypeQuery) OnlyX(ctx context.Context) *SemenFrozenType {
	node, err := sftq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SemenFrozenType ID in the query.
// Returns a *NotSingularError when more than one SemenFrozenType ID is found.
// Returns a *NotFoundError when no entities are found.
func (sftq *SemenFrozenTypeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sftq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{semenfrozentype.Label}
	default:
		err = &NotSingularError{semenfrozentype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sftq *SemenFrozenTypeQuery) OnlyIDX(ctx context.Context) int {
	id, err := sftq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SemenFrozenTypes.
func (sftq *SemenFrozenTypeQuery) All(ctx context.Context) ([]*SemenFrozenType, error) {
	if err := sftq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return sftq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (sftq *SemenFrozenTypeQuery) AllX(ctx context.Context) []*SemenFrozenType {
	nodes, err := sftq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SemenFrozenType IDs.
func (sftq *SemenFrozenTypeQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := sftq.Select(semenfrozentype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sftq *SemenFrozenTypeQuery) IDsX(ctx context.Context) []int {
	ids, err := sftq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sftq *SemenFrozenTypeQuery) Count(ctx context.Context) (int, error) {
	if err := sftq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return sftq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (sftq *SemenFrozenTypeQuery) CountX(ctx context.Context) int {
	count, err := sftq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sftq *SemenFrozenTypeQuery) Exist(ctx context.Context) (bool, error) {
	if err := sftq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return sftq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (sftq *SemenFrozenTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := sftq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SemenFrozenTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sftq *SemenFrozenTypeQuery) Clone() *SemenFrozenTypeQuery {
	if sftq == nil {
		return nil
	}
	return &SemenFrozenTypeQuery{
		config:     sftq.config,
		limit:      sftq.limit,
		offset:     sftq.offset,
		order:      append([]OrderFunc{}, sftq.order...),
		predicates: append([]predicate.SemenFrozenType{}, sftq.predicates...),
		// clone intermediate query.
		sql:    sftq.sql.Clone(),
		path:   sftq.path,
		unique: sftq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SemenFrozenType.Query().
//		GroupBy(semenfrozentype.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sftq *SemenFrozenTypeQuery) GroupBy(field string, fields ...string) *SemenFrozenTypeGroupBy {
	grbuild := &SemenFrozenTypeGroupBy{config: sftq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := sftq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return sftq.sqlQuery(ctx), nil
	}
	grbuild.label = semenfrozentype.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.SemenFrozenType.Query().
//		Select(semenfrozentype.FieldName).
//		Scan(ctx, &v)
func (sftq *SemenFrozenTypeQuery) Select(fields ...string) *SemenFrozenTypeSelect {
	sftq.fields = append(sftq.fields, fields...)
	selbuild := &SemenFrozenTypeSelect{SemenFrozenTypeQuery: sftq}
	selbuild.label = semenfrozentype.Label
	selbuild.flds, selbuild.scan = &sftq.fields, selbuild.Scan
	return selbuild
}

func (sftq *SemenFrozenTypeQuery) prepareQuery(ctx context.Context) error {
	for _, f := range sftq.fields {
		if !semenfrozentype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sftq.path != nil {
		prev, err := sftq.path(ctx)
		if err != nil {
			return err
		}
		sftq.sql = prev
	}
	return nil
}

func (sftq *SemenFrozenTypeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SemenFrozenType, error) {
	var (
		nodes = []*SemenFrozenType{}
		_spec = sftq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*SemenFrozenType).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &SemenFrozenType{config: sftq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sftq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (sftq *SemenFrozenTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sftq.querySpec()
	_spec.Node.Columns = sftq.fields
	if len(sftq.fields) > 0 {
		_spec.Unique = sftq.unique != nil && *sftq.unique
	}
	return sqlgraph.CountNodes(ctx, sftq.driver, _spec)
}

func (sftq *SemenFrozenTypeQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := sftq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (sftq *SemenFrozenTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   semenfrozentype.Table,
			Columns: semenfrozentype.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: semenfrozentype.FieldID,
			},
		},
		From:   sftq.sql,
		Unique: true,
	}
	if unique := sftq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := sftq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, semenfrozentype.FieldID)
		for i := range fields {
			if fields[i] != semenfrozentype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sftq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sftq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sftq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sftq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sftq *SemenFrozenTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sftq.driver.Dialect())
	t1 := builder.Table(semenfrozentype.Table)
	columns := sftq.fields
	if len(columns) == 0 {
		columns = semenfrozentype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sftq.sql != nil {
		selector = sftq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sftq.unique != nil && *sftq.unique {
		selector.Distinct()
	}
	for _, p := range sftq.predicates {
		p(selector)
	}
	for _, p := range sftq.order {
		p(selector)
	}
	if offset := sftq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sftq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SemenFrozenTypeGroupBy is the group-by builder for SemenFrozenType entities.
type SemenFrozenTypeGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sftgb *SemenFrozenTypeGroupBy) Aggregate(fns ...AggregateFunc) *SemenFrozenTypeGroupBy {
	sftgb.fns = append(sftgb.fns, fns...)
	return sftgb
}

// Scan applies the group-by query and scans the result into the given value.
func (sftgb *SemenFrozenTypeGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := sftgb.path(ctx)
	if err != nil {
		return err
	}
	sftgb.sql = query
	return sftgb.sqlScan(ctx, v)
}

func (sftgb *SemenFrozenTypeGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range sftgb.fields {
		if !semenfrozentype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := sftgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sftgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (sftgb *SemenFrozenTypeGroupBy) sqlQuery() *sql.Selector {
	selector := sftgb.sql.Select()
	aggregation := make([]string, 0, len(sftgb.fns))
	for _, fn := range sftgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(sftgb.fields)+len(sftgb.fns))
		for _, f := range sftgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(sftgb.fields...)...)
}

// SemenFrozenTypeSelect is the builder for selecting fields of SemenFrozenType entities.
type SemenFrozenTypeSelect struct {
	*SemenFrozenTypeQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (sfts *SemenFrozenTypeSelect) Scan(ctx context.Context, v interface{}) error {
	if err := sfts.prepareQuery(ctx); err != nil {
		return err
	}
	sfts.sql = sfts.SemenFrozenTypeQuery.sqlQuery(ctx)
	return sfts.sqlScan(ctx, v)
}

func (sfts *SemenFrozenTypeSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := sfts.sql.Query()
	if err := sfts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

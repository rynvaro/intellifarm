// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/shedtrans"
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// ShedTrans is the model entity for the ShedTrans schema.
type ShedTrans struct {
	config
	// ID of the ent.
	ID int `json:"id,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ShedTrans) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case shedtrans.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type ShedTrans", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ShedTrans fields.
func (st *ShedTrans) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case shedtrans.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			st.ID = int(value.Int64)
		}
	}
	return nil
}

// Update returns a builder for updating this ShedTrans.
// Note that you need to call ShedTrans.Unwrap() before calling this method if this ShedTrans
// was returned from a transaction, and the transaction was committed or rolled back.
func (st *ShedTrans) Update() *ShedTransUpdateOne {
	return (&ShedTransClient{config: st.config}).UpdateOne(st)
}

// Unwrap unwraps the ShedTrans entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (st *ShedTrans) Unwrap() *ShedTrans {
	_tx, ok := st.config.driver.(*txDriver)
	if !ok {
		panic("ent: ShedTrans is not a transactional entity")
	}
	st.config.driver = _tx.drv
	return st
}

// String implements the fmt.Stringer.
func (st *ShedTrans) String() string {
	var builder strings.Builder
	builder.WriteString("ShedTrans(")
	builder.WriteString(fmt.Sprintf("id=%v", st.ID))
	builder.WriteByte(')')
	return builder.String()
}

// ShedTransSlice is a parsable slice of ShedTrans.
type ShedTransSlice []*ShedTrans

func (st ShedTransSlice) config(cfg config) {
	for _i := range st {
		st[_i].config = cfg
	}
}

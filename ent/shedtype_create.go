// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/shedtype"
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ShedTypeCreate is the builder for creating a ShedType entity.
type ShedTypeCreate struct {
	config
	mutation *ShedTypeMutation
	hooks    []Hook
}

// SetName sets the "name" field.
func (stc *ShedTypeCreate) SetName(s string) *ShedTypeCreate {
	stc.mutation.SetName(s)
	return stc
}

// SetCode sets the "code" field.
func (stc *ShedTypeCreate) SetCode(s string) *ShedTypeCreate {
	stc.mutation.SetCode(s)
	return stc
}

// SetTenantId sets the "tenantId" field.
func (stc *ShedTypeCreate) SetTenantId(i int64) *ShedTypeCreate {
	stc.mutation.SetTenantId(i)
	return stc
}

// SetTenantName sets the "tenantName" field.
func (stc *ShedTypeCreate) SetTenantName(s string) *ShedTypeCreate {
	stc.mutation.SetTenantName(s)
	return stc
}

// SetOrder sets the "order" field.
func (stc *ShedTypeCreate) SetOrder(i int) *ShedTypeCreate {
	stc.mutation.SetOrder(i)
	return stc
}

// SetRemarks sets the "remarks" field.
func (stc *ShedTypeCreate) SetRemarks(s string) *ShedTypeCreate {
	stc.mutation.SetRemarks(s)
	return stc
}

// SetCreatedAt sets the "createdAt" field.
func (stc *ShedTypeCreate) SetCreatedAt(i int64) *ShedTypeCreate {
	stc.mutation.SetCreatedAt(i)
	return stc
}

// SetUpdatedAt sets the "updatedAt" field.
func (stc *ShedTypeCreate) SetUpdatedAt(i int64) *ShedTypeCreate {
	stc.mutation.SetUpdatedAt(i)
	return stc
}

// SetDeleted sets the "deleted" field.
func (stc *ShedTypeCreate) SetDeleted(i int) *ShedTypeCreate {
	stc.mutation.SetDeleted(i)
	return stc
}

// Mutation returns the ShedTypeMutation object of the builder.
func (stc *ShedTypeCreate) Mutation() *ShedTypeMutation {
	return stc.mutation
}

// Save creates the ShedType in the database.
func (stc *ShedTypeCreate) Save(ctx context.Context) (*ShedType, error) {
	var (
		err  error
		node *ShedType
	)
	if len(stc.hooks) == 0 {
		if err = stc.check(); err != nil {
			return nil, err
		}
		node, err = stc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ShedTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = stc.check(); err != nil {
				return nil, err
			}
			stc.mutation = mutation
			if node, err = stc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(stc.hooks) - 1; i >= 0; i-- {
			if stc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = stc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, stc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*ShedType)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from ShedTypeMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (stc *ShedTypeCreate) SaveX(ctx context.Context) *ShedType {
	v, err := stc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (stc *ShedTypeCreate) Exec(ctx context.Context) error {
	_, err := stc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (stc *ShedTypeCreate) ExecX(ctx context.Context) {
	if err := stc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (stc *ShedTypeCreate) check() error {
	if _, ok := stc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "ShedType.name"`)}
	}
	if _, ok := stc.mutation.Code(); !ok {
		return &ValidationError{Name: "code", err: errors.New(`ent: missing required field "ShedType.code"`)}
	}
	if _, ok := stc.mutation.TenantId(); !ok {
		return &ValidationError{Name: "tenantId", err: errors.New(`ent: missing required field "ShedType.tenantId"`)}
	}
	if _, ok := stc.mutation.TenantName(); !ok {
		return &ValidationError{Name: "tenantName", err: errors.New(`ent: missing required field "ShedType.tenantName"`)}
	}
	if _, ok := stc.mutation.Order(); !ok {
		return &ValidationError{Name: "order", err: errors.New(`ent: missing required field "ShedType.order"`)}
	}
	if _, ok := stc.mutation.Remarks(); !ok {
		return &ValidationError{Name: "remarks", err: errors.New(`ent: missing required field "ShedType.remarks"`)}
	}
	if _, ok := stc.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "createdAt", err: errors.New(`ent: missing required field "ShedType.createdAt"`)}
	}
	if _, ok := stc.mutation.UpdatedAt(); !ok {
		return &ValidationError{Name: "updatedAt", err: errors.New(`ent: missing required field "ShedType.updatedAt"`)}
	}
	if _, ok := stc.mutation.Deleted(); !ok {
		return &ValidationError{Name: "deleted", err: errors.New(`ent: missing required field "ShedType.deleted"`)}
	}
	return nil
}

func (stc *ShedTypeCreate) sqlSave(ctx context.Context) (*ShedType, error) {
	_node, _spec := stc.createSpec()
	if err := sqlgraph.CreateNode(ctx, stc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (stc *ShedTypeCreate) createSpec() (*ShedType, *sqlgraph.CreateSpec) {
	var (
		_node = &ShedType{config: stc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: shedtype.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: shedtype.FieldID,
			},
		}
	)
	if value, ok := stc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: shedtype.FieldName,
		})
		_node.Name = value
	}
	if value, ok := stc.mutation.Code(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: shedtype.FieldCode,
		})
		_node.Code = value
	}
	if value, ok := stc.mutation.TenantId(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt64,
			Value:  value,
			Column: shedtype.FieldTenantId,
		})
		_node.TenantId = value
	}
	if value, ok := stc.mutation.TenantName(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: shedtype.FieldTenantName,
		})
		_node.TenantName = value
	}
	if value, ok := stc.mutation.Order(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: shedtype.FieldOrder,
		})
		_node.Order = value
	}
	if value, ok := stc.mutation.Remarks(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: shedtype.FieldRemarks,
		})
		_node.Remarks = value
	}
	if value, ok := stc.mutation.CreatedAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt64,
			Value:  value,
			Column: shedtype.FieldCreatedAt,
		})
		_node.CreatedAt = value
	}
	if value, ok := stc.mutation.UpdatedAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt64,
			Value:  value,
			Column: shedtype.FieldUpdatedAt,
		})
		_node.UpdatedAt = value
	}
	if value, ok := stc.mutation.Deleted(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: shedtype.FieldDeleted,
		})
		_node.Deleted = value
	}
	return _node, _spec
}

// ShedTypeCreateBulk is the builder for creating many ShedType entities in bulk.
type ShedTypeCreateBulk struct {
	config
	builders []*ShedTypeCreate
}

// Save creates the ShedType entities in the database.
func (stcb *ShedTypeCreateBulk) Save(ctx context.Context) ([]*ShedType, error) {
	specs := make([]*sqlgraph.CreateSpec, len(stcb.builders))
	nodes := make([]*ShedType, len(stcb.builders))
	mutators := make([]Mutator, len(stcb.builders))
	for i := range stcb.builders {
		func(i int, root context.Context) {
			builder := stcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ShedTypeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, stcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, stcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, stcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (stcb *ShedTypeCreateBulk) SaveX(ctx context.Context) []*ShedType {
	v, err := stcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (stcb *ShedTypeCreateBulk) Exec(ctx context.Context) error {
	_, err := stcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (stcb *ShedTypeCreateBulk) ExecX(ctx context.Context) {
	if err := stcb.Exec(ctx); err != nil {
		panic(err)
	}
}

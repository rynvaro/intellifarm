// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/treatmentresult"
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// TreatmentResult is the model entity for the TreatmentResult schema.
type TreatmentResult struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 名称
	Name string `json:"name,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TreatmentResult) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case treatmentresult.FieldID:
			values[i] = new(sql.NullInt64)
		case treatmentresult.FieldName:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type TreatmentResult", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TreatmentResult fields.
func (tr *TreatmentResult) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case treatmentresult.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			tr.ID = int(value.Int64)
		case treatmentresult.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				tr.Name = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this TreatmentResult.
// Note that you need to call TreatmentResult.Unwrap() before calling this method if this TreatmentResult
// was returned from a transaction, and the transaction was committed or rolled back.
func (tr *TreatmentResult) Update() *TreatmentResultUpdateOne {
	return (&TreatmentResultClient{config: tr.config}).UpdateOne(tr)
}

// Unwrap unwraps the TreatmentResult entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tr *TreatmentResult) Unwrap() *TreatmentResult {
	_tx, ok := tr.config.driver.(*txDriver)
	if !ok {
		panic("ent: TreatmentResult is not a transactional entity")
	}
	tr.config.driver = _tx.drv
	return tr
}

// String implements the fmt.Stringer.
func (tr *TreatmentResult) String() string {
	var builder strings.Builder
	builder.WriteString("TreatmentResult(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tr.ID))
	builder.WriteString("name=")
	builder.WriteString(tr.Name)
	builder.WriteByte(')')
	return builder.String()
}

// TreatmentResults is a parsable slice of TreatmentResult.
type TreatmentResults []*TreatmentResult

func (tr TreatmentResults) config(cfg config) {
	for _i := range tr {
		tr[_i].config = cfg
	}
}

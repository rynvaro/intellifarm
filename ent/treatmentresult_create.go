// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/treatmentresult"
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
)

// TreatmentResultCreate is the builder for creating a TreatmentResult entity.
type TreatmentResultCreate struct {
	config
	mutation *TreatmentResultMutation
	hooks    []Hook
}

// SetName sets the name field.
func (trc *TreatmentResultCreate) SetName(s string) *TreatmentResultCreate {
	trc.mutation.SetName(s)
	return trc
}

// Mutation returns the TreatmentResultMutation object of the builder.
func (trc *TreatmentResultCreate) Mutation() *TreatmentResultMutation {
	return trc.mutation
}

// Save creates the TreatmentResult in the database.
func (trc *TreatmentResultCreate) Save(ctx context.Context) (*TreatmentResult, error) {
	var (
		err  error
		node *TreatmentResult
	)
	if len(trc.hooks) == 0 {
		if err = trc.check(); err != nil {
			return nil, err
		}
		node, err = trc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*TreatmentResultMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = trc.check(); err != nil {
				return nil, err
			}
			trc.mutation = mutation
			node, err = trc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(trc.hooks) - 1; i >= 0; i-- {
			mut = trc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, trc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (trc *TreatmentResultCreate) SaveX(ctx context.Context) *TreatmentResult {
	v, err := trc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (trc *TreatmentResultCreate) check() error {
	if _, ok := trc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := trc.mutation.Name(); ok {
		if err := treatmentresult.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (trc *TreatmentResultCreate) sqlSave(ctx context.Context) (*TreatmentResult, error) {
	_node, _spec := trc.createSpec()
	if err := sqlgraph.CreateNode(ctx, trc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int64(id)
	return _node, nil
}

func (trc *TreatmentResultCreate) createSpec() (*TreatmentResult, *sqlgraph.CreateSpec) {
	var (
		_node = &TreatmentResult{config: trc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: treatmentresult.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: treatmentresult.FieldID,
			},
		}
	)
	if value, ok := trc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: treatmentresult.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// TreatmentResultCreateBulk is the builder for creating a bulk of TreatmentResult entities.
type TreatmentResultCreateBulk struct {
	config
	builders []*TreatmentResultCreate
}

// Save creates the TreatmentResult entities in the database.
func (trcb *TreatmentResultCreateBulk) Save(ctx context.Context) ([]*TreatmentResult, error) {
	specs := make([]*sqlgraph.CreateSpec, len(trcb.builders))
	nodes := make([]*TreatmentResult, len(trcb.builders))
	mutators := make([]Mutator, len(trcb.builders))
	for i := range trcb.builders {
		func(i int, root context.Context) {
			builder := trcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*TreatmentResultMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, trcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, trcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int64(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, trcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (trcb *TreatmentResultCreateBulk) SaveX(ctx context.Context) []*TreatmentResult {
	v, err := trcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

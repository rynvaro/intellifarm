// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/predicate"
	"cattleai/ent/veterinarydrugsinfo"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// VeterinaryDrugsInfoQuery is the builder for querying VeterinaryDrugsInfo entities.
type VeterinaryDrugsInfoQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.VeterinaryDrugsInfo
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the VeterinaryDrugsInfoQuery builder.
func (vdiq *VeterinaryDrugsInfoQuery) Where(ps ...predicate.VeterinaryDrugsInfo) *VeterinaryDrugsInfoQuery {
	vdiq.predicates = append(vdiq.predicates, ps...)
	return vdiq
}

// Limit adds a limit step to the query.
func (vdiq *VeterinaryDrugsInfoQuery) Limit(limit int) *VeterinaryDrugsInfoQuery {
	vdiq.limit = &limit
	return vdiq
}

// Offset adds an offset step to the query.
func (vdiq *VeterinaryDrugsInfoQuery) Offset(offset int) *VeterinaryDrugsInfoQuery {
	vdiq.offset = &offset
	return vdiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (vdiq *VeterinaryDrugsInfoQuery) Unique(unique bool) *VeterinaryDrugsInfoQuery {
	vdiq.unique = &unique
	return vdiq
}

// Order adds an order step to the query.
func (vdiq *VeterinaryDrugsInfoQuery) Order(o ...OrderFunc) *VeterinaryDrugsInfoQuery {
	vdiq.order = append(vdiq.order, o...)
	return vdiq
}

// First returns the first VeterinaryDrugsInfo entity from the query.
// Returns a *NotFoundError when no VeterinaryDrugsInfo was found.
func (vdiq *VeterinaryDrugsInfoQuery) First(ctx context.Context) (*VeterinaryDrugsInfo, error) {
	nodes, err := vdiq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{veterinarydrugsinfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (vdiq *VeterinaryDrugsInfoQuery) FirstX(ctx context.Context) *VeterinaryDrugsInfo {
	node, err := vdiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first VeterinaryDrugsInfo ID from the query.
// Returns a *NotFoundError when no VeterinaryDrugsInfo ID was found.
func (vdiq *VeterinaryDrugsInfoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vdiq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{veterinarydrugsinfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (vdiq *VeterinaryDrugsInfoQuery) FirstIDX(ctx context.Context) int {
	id, err := vdiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single VeterinaryDrugsInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one VeterinaryDrugsInfo entity is found.
// Returns a *NotFoundError when no VeterinaryDrugsInfo entities are found.
func (vdiq *VeterinaryDrugsInfoQuery) Only(ctx context.Context) (*VeterinaryDrugsInfo, error) {
	nodes, err := vdiq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{veterinarydrugsinfo.Label}
	default:
		return nil, &NotSingularError{veterinarydrugsinfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (vdiq *VeterinaryDrugsInfoQuery) OnlyX(ctx context.Context) *VeterinaryDrugsInfo {
	node, err := vdiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only VeterinaryDrugsInfo ID in the query.
// Returns a *NotSingularError when more than one VeterinaryDrugsInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (vdiq *VeterinaryDrugsInfoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vdiq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{veterinarydrugsinfo.Label}
	default:
		err = &NotSingularError{veterinarydrugsinfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (vdiq *VeterinaryDrugsInfoQuery) OnlyIDX(ctx context.Context) int {
	id, err := vdiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of VeterinaryDrugsInfos.
func (vdiq *VeterinaryDrugsInfoQuery) All(ctx context.Context) ([]*VeterinaryDrugsInfo, error) {
	if err := vdiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return vdiq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (vdiq *VeterinaryDrugsInfoQuery) AllX(ctx context.Context) []*VeterinaryDrugsInfo {
	nodes, err := vdiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of VeterinaryDrugsInfo IDs.
func (vdiq *VeterinaryDrugsInfoQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := vdiq.Select(veterinarydrugsinfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (vdiq *VeterinaryDrugsInfoQuery) IDsX(ctx context.Context) []int {
	ids, err := vdiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (vdiq *VeterinaryDrugsInfoQuery) Count(ctx context.Context) (int, error) {
	if err := vdiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return vdiq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (vdiq *VeterinaryDrugsInfoQuery) CountX(ctx context.Context) int {
	count, err := vdiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (vdiq *VeterinaryDrugsInfoQuery) Exist(ctx context.Context) (bool, error) {
	if err := vdiq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return vdiq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (vdiq *VeterinaryDrugsInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := vdiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the VeterinaryDrugsInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (vdiq *VeterinaryDrugsInfoQuery) Clone() *VeterinaryDrugsInfoQuery {
	if vdiq == nil {
		return nil
	}
	return &VeterinaryDrugsInfoQuery{
		config:     vdiq.config,
		limit:      vdiq.limit,
		offset:     vdiq.offset,
		order:      append([]OrderFunc{}, vdiq.order...),
		predicates: append([]predicate.VeterinaryDrugsInfo{}, vdiq.predicates...),
		// clone intermediate query.
		sql:    vdiq.sql.Clone(),
		path:   vdiq.path,
		unique: vdiq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.VeterinaryDrugsInfo.Query().
//		GroupBy(veterinarydrugsinfo.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (vdiq *VeterinaryDrugsInfoQuery) GroupBy(field string, fields ...string) *VeterinaryDrugsInfoGroupBy {
	grbuild := &VeterinaryDrugsInfoGroupBy{config: vdiq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := vdiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return vdiq.sqlQuery(ctx), nil
	}
	grbuild.label = veterinarydrugsinfo.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.VeterinaryDrugsInfo.Query().
//		Select(veterinarydrugsinfo.FieldName).
//		Scan(ctx, &v)
func (vdiq *VeterinaryDrugsInfoQuery) Select(fields ...string) *VeterinaryDrugsInfoSelect {
	vdiq.fields = append(vdiq.fields, fields...)
	selbuild := &VeterinaryDrugsInfoSelect{VeterinaryDrugsInfoQuery: vdiq}
	selbuild.label = veterinarydrugsinfo.Label
	selbuild.flds, selbuild.scan = &vdiq.fields, selbuild.Scan
	return selbuild
}

func (vdiq *VeterinaryDrugsInfoQuery) prepareQuery(ctx context.Context) error {
	for _, f := range vdiq.fields {
		if !veterinarydrugsinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if vdiq.path != nil {
		prev, err := vdiq.path(ctx)
		if err != nil {
			return err
		}
		vdiq.sql = prev
	}
	return nil
}

func (vdiq *VeterinaryDrugsInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*VeterinaryDrugsInfo, error) {
	var (
		nodes = []*VeterinaryDrugsInfo{}
		_spec = vdiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*VeterinaryDrugsInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &VeterinaryDrugsInfo{config: vdiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, vdiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (vdiq *VeterinaryDrugsInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := vdiq.querySpec()
	_spec.Node.Columns = vdiq.fields
	if len(vdiq.fields) > 0 {
		_spec.Unique = vdiq.unique != nil && *vdiq.unique
	}
	return sqlgraph.CountNodes(ctx, vdiq.driver, _spec)
}

func (vdiq *VeterinaryDrugsInfoQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := vdiq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (vdiq *VeterinaryDrugsInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   veterinarydrugsinfo.Table,
			Columns: veterinarydrugsinfo.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: veterinarydrugsinfo.FieldID,
			},
		},
		From:   vdiq.sql,
		Unique: true,
	}
	if unique := vdiq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := vdiq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, veterinarydrugsinfo.FieldID)
		for i := range fields {
			if fields[i] != veterinarydrugsinfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := vdiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := vdiq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := vdiq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := vdiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (vdiq *VeterinaryDrugsInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(vdiq.driver.Dialect())
	t1 := builder.Table(veterinarydrugsinfo.Table)
	columns := vdiq.fields
	if len(columns) == 0 {
		columns = veterinarydrugsinfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if vdiq.sql != nil {
		selector = vdiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if vdiq.unique != nil && *vdiq.unique {
		selector.Distinct()
	}
	for _, p := range vdiq.predicates {
		p(selector)
	}
	for _, p := range vdiq.order {
		p(selector)
	}
	if offset := vdiq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := vdiq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// VeterinaryDrugsInfoGroupBy is the group-by builder for VeterinaryDrugsInfo entities.
type VeterinaryDrugsInfoGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (vdigb *VeterinaryDrugsInfoGroupBy) Aggregate(fns ...AggregateFunc) *VeterinaryDrugsInfoGroupBy {
	vdigb.fns = append(vdigb.fns, fns...)
	return vdigb
}

// Scan applies the group-by query and scans the result into the given value.
func (vdigb *VeterinaryDrugsInfoGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := vdigb.path(ctx)
	if err != nil {
		return err
	}
	vdigb.sql = query
	return vdigb.sqlScan(ctx, v)
}

func (vdigb *VeterinaryDrugsInfoGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range vdigb.fields {
		if !veterinarydrugsinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := vdigb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vdigb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (vdigb *VeterinaryDrugsInfoGroupBy) sqlQuery() *sql.Selector {
	selector := vdigb.sql.Select()
	aggregation := make([]string, 0, len(vdigb.fns))
	for _, fn := range vdigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(vdigb.fields)+len(vdigb.fns))
		for _, f := range vdigb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(vdigb.fields...)...)
}

// VeterinaryDrugsInfoSelect is the builder for selecting fields of VeterinaryDrugsInfo entities.
type VeterinaryDrugsInfoSelect struct {
	*VeterinaryDrugsInfoQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (vdis *VeterinaryDrugsInfoSelect) Scan(ctx context.Context, v interface{}) error {
	if err := vdis.prepareQuery(ctx); err != nil {
		return err
	}
	vdis.sql = vdis.VeterinaryDrugsInfoQuery.sqlQuery(ctx)
	return vdis.sqlScan(ctx, v)
}

func (vdis *VeterinaryDrugsInfoSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := vdis.sql.Query()
	if err := vdis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

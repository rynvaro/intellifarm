// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/whereabouts"
	"fmt"
	"strings"

	"github.com/facebook/ent/dialect/sql"
)

// Whereabouts is the model entity for the Whereabouts schema.
type Whereabouts struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Whereabouts) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // name
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Whereabouts fields.
func (w *Whereabouts) assignValues(values ...interface{}) error {
	if m, n := len(values), len(whereabouts.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	w.ID = int64(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field name", values[0])
	} else if value.Valid {
		w.Name = value.String
	}
	return nil
}

// Update returns a builder for updating this Whereabouts.
// Note that, you need to call Whereabouts.Unwrap() before calling this method, if this Whereabouts
// was returned from a transaction, and the transaction was committed or rolled back.
func (w *Whereabouts) Update() *WhereaboutsUpdateOne {
	return (&WhereaboutsClient{config: w.config}).UpdateOne(w)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (w *Whereabouts) Unwrap() *Whereabouts {
	tx, ok := w.config.driver.(*txDriver)
	if !ok {
		panic("ent: Whereabouts is not a transactional entity")
	}
	w.config.driver = tx.drv
	return w
}

// String implements the fmt.Stringer.
func (w *Whereabouts) String() string {
	var builder strings.Builder
	builder.WriteString("Whereabouts(")
	builder.WriteString(fmt.Sprintf("id=%v", w.ID))
	builder.WriteString(", name=")
	builder.WriteString(w.Name)
	builder.WriteByte(')')
	return builder.String()
}

// WhereaboutsSlice is a parsable slice of Whereabouts.
type WhereaboutsSlice []*Whereabouts

func (w WhereaboutsSlice) config(cfg config) {
	for _i := range w {
		w[_i].config = cfg
	}
}

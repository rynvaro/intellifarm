// Code generated by entc, DO NOT EDIT.

package ent

import (
	"cattleai/ent/whereabouts"
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
)

// WhereaboutsCreate is the builder for creating a Whereabouts entity.
type WhereaboutsCreate struct {
	config
	mutation *WhereaboutsMutation
	hooks    []Hook
}

// SetName sets the name field.
func (wc *WhereaboutsCreate) SetName(s string) *WhereaboutsCreate {
	wc.mutation.SetName(s)
	return wc
}

// Mutation returns the WhereaboutsMutation object of the builder.
func (wc *WhereaboutsCreate) Mutation() *WhereaboutsMutation {
	return wc.mutation
}

// Save creates the Whereabouts in the database.
func (wc *WhereaboutsCreate) Save(ctx context.Context) (*Whereabouts, error) {
	var (
		err  error
		node *Whereabouts
	)
	if len(wc.hooks) == 0 {
		if err = wc.check(); err != nil {
			return nil, err
		}
		node, err = wc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*WhereaboutsMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = wc.check(); err != nil {
				return nil, err
			}
			wc.mutation = mutation
			node, err = wc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(wc.hooks) - 1; i >= 0; i-- {
			mut = wc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, wc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (wc *WhereaboutsCreate) SaveX(ctx context.Context) *Whereabouts {
	v, err := wc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (wc *WhereaboutsCreate) check() error {
	if _, ok := wc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := wc.mutation.Name(); ok {
		if err := whereabouts.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (wc *WhereaboutsCreate) sqlSave(ctx context.Context) (*Whereabouts, error) {
	_node, _spec := wc.createSpec()
	if err := sqlgraph.CreateNode(ctx, wc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int64(id)
	return _node, nil
}

func (wc *WhereaboutsCreate) createSpec() (*Whereabouts, *sqlgraph.CreateSpec) {
	var (
		_node = &Whereabouts{config: wc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: whereabouts.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: whereabouts.FieldID,
			},
		}
	)
	if value, ok := wc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: whereabouts.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// WhereaboutsCreateBulk is the builder for creating a bulk of Whereabouts entities.
type WhereaboutsCreateBulk struct {
	config
	builders []*WhereaboutsCreate
}

// Save creates the Whereabouts entities in the database.
func (wcb *WhereaboutsCreateBulk) Save(ctx context.Context) ([]*Whereabouts, error) {
	specs := make([]*sqlgraph.CreateSpec, len(wcb.builders))
	nodes := make([]*Whereabouts, len(wcb.builders))
	mutators := make([]Mutator, len(wcb.builders))
	for i := range wcb.builders {
		func(i int, root context.Context) {
			builder := wcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*WhereaboutsMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, wcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, wcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int64(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, wcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (wcb *WhereaboutsCreateBulk) SaveX(ctx context.Context) []*Whereabouts {
	v, err := wcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

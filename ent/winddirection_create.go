// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/winddirection"
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WindDirectionCreate is the builder for creating a WindDirection entity.
type WindDirectionCreate struct {
	config
	mutation *WindDirectionMutation
	hooks    []Hook
}

// SetName sets the "name" field.
func (wdc *WindDirectionCreate) SetName(s string) *WindDirectionCreate {
	wdc.mutation.SetName(s)
	return wdc
}

// Mutation returns the WindDirectionMutation object of the builder.
func (wdc *WindDirectionCreate) Mutation() *WindDirectionMutation {
	return wdc.mutation
}

// Save creates the WindDirection in the database.
func (wdc *WindDirectionCreate) Save(ctx context.Context) (*WindDirection, error) {
	var (
		err  error
		node *WindDirection
	)
	if len(wdc.hooks) == 0 {
		if err = wdc.check(); err != nil {
			return nil, err
		}
		node, err = wdc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*WindDirectionMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = wdc.check(); err != nil {
				return nil, err
			}
			wdc.mutation = mutation
			if node, err = wdc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(wdc.hooks) - 1; i >= 0; i-- {
			if wdc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = wdc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, wdc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*WindDirection)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from WindDirectionMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (wdc *WindDirectionCreate) SaveX(ctx context.Context) *WindDirection {
	v, err := wdc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (wdc *WindDirectionCreate) Exec(ctx context.Context) error {
	_, err := wdc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (wdc *WindDirectionCreate) ExecX(ctx context.Context) {
	if err := wdc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (wdc *WindDirectionCreate) check() error {
	if _, ok := wdc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "WindDirection.name"`)}
	}
	if v, ok := wdc.mutation.Name(); ok {
		if err := winddirection.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf(`ent: validator failed for field "WindDirection.name": %w`, err)}
		}
	}
	return nil
}

func (wdc *WindDirectionCreate) sqlSave(ctx context.Context) (*WindDirection, error) {
	_node, _spec := wdc.createSpec()
	if err := sqlgraph.CreateNode(ctx, wdc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (wdc *WindDirectionCreate) createSpec() (*WindDirection, *sqlgraph.CreateSpec) {
	var (
		_node = &WindDirection{config: wdc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: winddirection.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: winddirection.FieldID,
			},
		}
	)
	if value, ok := wdc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: winddirection.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// WindDirectionCreateBulk is the builder for creating many WindDirection entities in bulk.
type WindDirectionCreateBulk struct {
	config
	builders []*WindDirectionCreate
}

// Save creates the WindDirection entities in the database.
func (wdcb *WindDirectionCreateBulk) Save(ctx context.Context) ([]*WindDirection, error) {
	specs := make([]*sqlgraph.CreateSpec, len(wdcb.builders))
	nodes := make([]*WindDirection, len(wdcb.builders))
	mutators := make([]Mutator, len(wdcb.builders))
	for i := range wdcb.builders {
		func(i int, root context.Context) {
			builder := wdcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*WindDirectionMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, wdcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, wdcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, wdcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (wdcb *WindDirectionCreateBulk) SaveX(ctx context.Context) []*WindDirection {
	v, err := wdcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (wdcb *WindDirectionCreateBulk) Exec(ctx context.Context) error {
	_, err := wdcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (wdcb *WindDirectionCreateBulk) ExecX(ctx context.Context) {
	if err := wdcb.Exec(ctx); err != nil {
		panic(err)
	}
}

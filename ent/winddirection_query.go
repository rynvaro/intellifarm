// Code generated by ent, DO NOT EDIT.

package ent

import (
	"cattleai/ent/predicate"
	"cattleai/ent/winddirection"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WindDirectionQuery is the builder for querying WindDirection entities.
type WindDirectionQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.WindDirection
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WindDirectionQuery builder.
func (wdq *WindDirectionQuery) Where(ps ...predicate.WindDirection) *WindDirectionQuery {
	wdq.predicates = append(wdq.predicates, ps...)
	return wdq
}

// Limit adds a limit step to the query.
func (wdq *WindDirectionQuery) Limit(limit int) *WindDirectionQuery {
	wdq.limit = &limit
	return wdq
}

// Offset adds an offset step to the query.
func (wdq *WindDirectionQuery) Offset(offset int) *WindDirectionQuery {
	wdq.offset = &offset
	return wdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wdq *WindDirectionQuery) Unique(unique bool) *WindDirectionQuery {
	wdq.unique = &unique
	return wdq
}

// Order adds an order step to the query.
func (wdq *WindDirectionQuery) Order(o ...OrderFunc) *WindDirectionQuery {
	wdq.order = append(wdq.order, o...)
	return wdq
}

// First returns the first WindDirection entity from the query.
// Returns a *NotFoundError when no WindDirection was found.
func (wdq *WindDirectionQuery) First(ctx context.Context) (*WindDirection, error) {
	nodes, err := wdq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{winddirection.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wdq *WindDirectionQuery) FirstX(ctx context.Context) *WindDirection {
	node, err := wdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WindDirection ID from the query.
// Returns a *NotFoundError when no WindDirection ID was found.
func (wdq *WindDirectionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = wdq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{winddirection.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wdq *WindDirectionQuery) FirstIDX(ctx context.Context) int {
	id, err := wdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WindDirection entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WindDirection entity is found.
// Returns a *NotFoundError when no WindDirection entities are found.
func (wdq *WindDirectionQuery) Only(ctx context.Context) (*WindDirection, error) {
	nodes, err := wdq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{winddirection.Label}
	default:
		return nil, &NotSingularError{winddirection.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wdq *WindDirectionQuery) OnlyX(ctx context.Context) *WindDirection {
	node, err := wdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WindDirection ID in the query.
// Returns a *NotSingularError when more than one WindDirection ID is found.
// Returns a *NotFoundError when no entities are found.
func (wdq *WindDirectionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = wdq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{winddirection.Label}
	default:
		err = &NotSingularError{winddirection.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wdq *WindDirectionQuery) OnlyIDX(ctx context.Context) int {
	id, err := wdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WindDirections.
func (wdq *WindDirectionQuery) All(ctx context.Context) ([]*WindDirection, error) {
	if err := wdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return wdq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (wdq *WindDirectionQuery) AllX(ctx context.Context) []*WindDirection {
	nodes, err := wdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WindDirection IDs.
func (wdq *WindDirectionQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := wdq.Select(winddirection.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wdq *WindDirectionQuery) IDsX(ctx context.Context) []int {
	ids, err := wdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wdq *WindDirectionQuery) Count(ctx context.Context) (int, error) {
	if err := wdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return wdq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (wdq *WindDirectionQuery) CountX(ctx context.Context) int {
	count, err := wdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wdq *WindDirectionQuery) Exist(ctx context.Context) (bool, error) {
	if err := wdq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return wdq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (wdq *WindDirectionQuery) ExistX(ctx context.Context) bool {
	exist, err := wdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WindDirectionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wdq *WindDirectionQuery) Clone() *WindDirectionQuery {
	if wdq == nil {
		return nil
	}
	return &WindDirectionQuery{
		config:     wdq.config,
		limit:      wdq.limit,
		offset:     wdq.offset,
		order:      append([]OrderFunc{}, wdq.order...),
		predicates: append([]predicate.WindDirection{}, wdq.predicates...),
		// clone intermediate query.
		sql:    wdq.sql.Clone(),
		path:   wdq.path,
		unique: wdq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WindDirection.Query().
//		GroupBy(winddirection.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wdq *WindDirectionQuery) GroupBy(field string, fields ...string) *WindDirectionGroupBy {
	grbuild := &WindDirectionGroupBy{config: wdq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := wdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return wdq.sqlQuery(ctx), nil
	}
	grbuild.label = winddirection.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.WindDirection.Query().
//		Select(winddirection.FieldName).
//		Scan(ctx, &v)
func (wdq *WindDirectionQuery) Select(fields ...string) *WindDirectionSelect {
	wdq.fields = append(wdq.fields, fields...)
	selbuild := &WindDirectionSelect{WindDirectionQuery: wdq}
	selbuild.label = winddirection.Label
	selbuild.flds, selbuild.scan = &wdq.fields, selbuild.Scan
	return selbuild
}

func (wdq *WindDirectionQuery) prepareQuery(ctx context.Context) error {
	for _, f := range wdq.fields {
		if !winddirection.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wdq.path != nil {
		prev, err := wdq.path(ctx)
		if err != nil {
			return err
		}
		wdq.sql = prev
	}
	return nil
}

func (wdq *WindDirectionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WindDirection, error) {
	var (
		nodes = []*WindDirection{}
		_spec = wdq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*WindDirection).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &WindDirection{config: wdq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wdq *WindDirectionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wdq.querySpec()
	_spec.Node.Columns = wdq.fields
	if len(wdq.fields) > 0 {
		_spec.Unique = wdq.unique != nil && *wdq.unique
	}
	return sqlgraph.CountNodes(ctx, wdq.driver, _spec)
}

func (wdq *WindDirectionQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := wdq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (wdq *WindDirectionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   winddirection.Table,
			Columns: winddirection.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: winddirection.FieldID,
			},
		},
		From:   wdq.sql,
		Unique: true,
	}
	if unique := wdq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := wdq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, winddirection.FieldID)
		for i := range fields {
			if fields[i] != winddirection.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wdq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wdq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wdq *WindDirectionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wdq.driver.Dialect())
	t1 := builder.Table(winddirection.Table)
	columns := wdq.fields
	if len(columns) == 0 {
		columns = winddirection.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wdq.sql != nil {
		selector = wdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wdq.unique != nil && *wdq.unique {
		selector.Distinct()
	}
	for _, p := range wdq.predicates {
		p(selector)
	}
	for _, p := range wdq.order {
		p(selector)
	}
	if offset := wdq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wdq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WindDirectionGroupBy is the group-by builder for WindDirection entities.
type WindDirectionGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wdgb *WindDirectionGroupBy) Aggregate(fns ...AggregateFunc) *WindDirectionGroupBy {
	wdgb.fns = append(wdgb.fns, fns...)
	return wdgb
}

// Scan applies the group-by query and scans the result into the given value.
func (wdgb *WindDirectionGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := wdgb.path(ctx)
	if err != nil {
		return err
	}
	wdgb.sql = query
	return wdgb.sqlScan(ctx, v)
}

func (wdgb *WindDirectionGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range wdgb.fields {
		if !winddirection.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := wdgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wdgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (wdgb *WindDirectionGroupBy) sqlQuery() *sql.Selector {
	selector := wdgb.sql.Select()
	aggregation := make([]string, 0, len(wdgb.fns))
	for _, fn := range wdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(wdgb.fields)+len(wdgb.fns))
		for _, f := range wdgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(wdgb.fields...)...)
}

// WindDirectionSelect is the builder for selecting fields of WindDirection entities.
type WindDirectionSelect struct {
	*WindDirectionQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (wds *WindDirectionSelect) Scan(ctx context.Context, v interface{}) error {
	if err := wds.prepareQuery(ctx); err != nil {
		return err
	}
	wds.sql = wds.WindDirectionQuery.sqlQuery(ctx)
	return wds.sqlScan(ctx, v)
}

func (wds *WindDirectionSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := wds.sql.Query()
	if err := wds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
